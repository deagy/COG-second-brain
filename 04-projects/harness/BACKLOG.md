# Harness backlog

Action items from retros. `AI-n` ids are scoped to their retro.

## The three dispositions

Every item is one of three things, and the difference decides what happens next. A backlog where every row reads `open` cannot tell them apart, so nothing gets done: an unbuildable rule and an unbuilt one look identical, and both wait.

| Disposition | Means | What closes it |
|---|---|---|
| **control** | A check can observe this defect | Building the check and falsifying it in both directions: the defect reintroduced and the check seen failing, then removed and seen passing. A check seen only passing is not evidence |
| **advice** | No check reaches it, for a reason specific to this item | Landing the rule somewhere read at session start — `CLAUDE.md` or a skill body — with its originating incident attached. A rule without its incident is a maxim, and a maxim gets skimmed |
| **landed** | Already done | Evidence that the artifact exists, named. Not "it looks done" |

**A `control` row cites where its check lives — a file, and a commit for anything shipped.** A `control` with no citation is *unbuilt*, and that is the state this file exists to make visible: the difference between a rule nobody has built yet and one nobody can build. Write `control — unbuilt` while it waits, so the distinction survives someone reading only this table.

An `advice` reason has to answer **what a check would have to observe, and why that is unobservable.** A reason that would fit several items is not a reason for one of them. And a *cost* argument — a check is possible but not worth its infrastructure — must say so plainly rather than dressing itself as impossibility, or nobody can revisit it when the cost changes.

Dispositioned by the `controls-not-advice` ultragoal (`04-projects/controls-not-advice/`), whose own premise is why this section exists: AI-5 sat open for four days as a working-practice note, and the next ultragoal committed the exact error it named.

| ID | From | Action | Target | Disposition |
|---|---|---|---|---|
| AI-1 | [2026-08-29 P1](retro/2026-08-29-repo-consolidation-p1.md) | Extraction and port plans inventory four things: imports, prose mentions, **data readers**, **releasable-component models**. Apply before the P3 plan is written. | P3 plan | **control** — merged with AI-11; `.claude/lib/evidence-lint.sh` axes check, commit `44af752`. Residual advice: the check reads whether an axis is present, never whether it was honestly investigated |
| AI-2 | [2026-08-29 P1](retro/2026-08-29-repo-consolidation-p1.md) | Verify a naming or destination target exists / is free before putting the decision to the user. | working practice | **advice** — landed in `CLAUDE.md` § Before you assert it, check it. No artifact: the defect is in a message to the user |
| AI-3 | [2026-08-29 P1](retro/2026-08-29-repo-consolidation-p1.md) | Tests resolving external tooling require an already-built artifact and skip; never build on demand inside a package whose other tests narrow the environment. | working practice | **control** — cadre `internal/cli/toolchain_guards_test.go`, commit `fd2c2295`. Three live defects fixed |
| AI-4 | [2026-08-29 P1](retro/2026-08-29-repo-consolidation-p1.md) | Acceptance criteria must be satisfiable within the phase that owns them. | `.claude/skills/ultragoal/SKILL.md` § Phase 0 | **control** ×2 — `.claude/lib/spec-lint.sh`, commit `f322c19` |
| AI-5 | [2026-08-29 P1](retro/2026-08-29-repo-consolidation-p1.md) | Decide whether CP-4 applies to a first phase, or state it begins at P2. Skipped by omission in P1. | `.claude/skills/ultragoal/SKILL.md` § phase loop | **control** — `.claude/lib/phase-gates.sh`, commit `181cecb`. CP-4 is owed when a phase names more than one task |
| AI-6 | [2026-08-29 P2](retro/2026-08-29-repo-consolidation-p2.md) | Where a criterion's literal and intended readings differ, the literal one governs, or rewrite the criterion before the phase closes. Never read generously in your own favour at gate time. | `.claude/skills/ultragoal/SKILL.md` § acceptance gates | **landed** (`WORKFLOW.md` § Amending a gated criterion) + **advice** (§ Reading a criterion you are about to be judged by): no syntactic pattern separates a generous reading from a correct one — the criterion is unedited and the verdict is a judgment, so the harness answers it structurally instead, with a verifier who did not write the thing being judged |
| AI-7 | [2026-08-29 P2](retro/2026-08-29-repo-consolidation-p2.md) | Never put a file write and the commit describing it in one compound command. Write, confirm, then commit. | working practice | **advice** — `CLAUDE.md` § Git. A hook could enforce it; COG ships none, so this is a cost decision, not an impossibility |
| AI-8 | [2026-08-29 P2](retro/2026-08-29-repo-consolidation-p2.md) | Assess salvage value from the working tree as well as history. | working practice | **control** — `.claude/lib/evidence-lint.sh`, commit `44af752` |
| AI-9 | [2026-08-29 P2](retro/2026-08-29-repo-consolidation-p2.md) | Check repository visibility before reasoning about who its documentation reaches. | working practice | **advice** — `CLAUDE.md`. Scriptable, but nothing would invoke it: the defect is a reasoning step |
| AI-10 | [P3 T-05 defect](../repo-consolidation/evidence/P3/CP-3-T05-defect.md) | An inventory piped through `head` is not an inventory. Count the hits first, then read all of them. Truncation is invisible in the output and reads as completeness. | working practice | **control** — `.claude/lib/evidence-lint.sh`, commit `44af752`, scoped to evidence documents |
| AI-11 | [2026-08-29 P3](retro/2026-08-29-repo-consolidation-p3.md) | Add a fifth inventory axis: what does the destination already do? AI-1's four describe what moves; none look at what it moves into. | migration plans | **control** — merged with AI-1 as the fifth inventory axis |
| AI-12 | [2026-08-29 P3](retro/2026-08-29-repo-consolidation-p3.md) | When a correction lands in code, find its twin in prose in the same change — changelog, doc comment, commit message, README. | working practice | **control** — gloop `internal/docguard/deprecation_parity_test.go`, commit `0088da3` |
| AI-13 | [2026-08-29 P3](retro/2026-08-29-repo-consolidation-p3.md) | A guard resolving an external tool must report which one it resolved. A pass that does not say what it checked is not evidence. | working practice | **control** — cadre `internal/cli/toolchain_guards_test.go`, commit `fd2c2295`. Narrower than the retro incident: it guards the resolved path, not the version |
| AI-14 | [2026-08-29 P3](retro/2026-08-29-repo-consolidation-p3.md) | Treat an environment note as a finding until shown otherwise. | working practice | **advice** — `CLAUDE.md`. AI-13 does **not** cover its originating instance: that turned on an unlogged *version*, and AI-13 guards a discarded *path*. Nothing covers the version case |
| AI-15 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | CP-4 run retrospectively on capability-parity: FAIL:fixable, found the AC-7 drift guard never scanned the repository root, so AC-2's corrections to `RELEASE_NOTES_PHASE4.md` and `PHASE4_ROADMAP.md` had no regression protection. Fixed in cadre `f378fee1`; `phase-gates.sh` added and wired into the north-star gate. **Corrected:** the original wording claimed CP-4 never runs and proposed deleting it — false; repo-consolidation ran it five times. | `.claude/skills/ultragoal/SKILL.md` § phase loop | **landed** — CP-4 run, the AC-7 guard widened (cadre `f378fee1`), and `phase-gates.sh` added so a skipped checkpoint is detectable |
| AI-16 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | Before editing to satisfy a criterion that says *every* document, build the enumeration first and enumerate by capability **concept**, not by identifier. Documents assert capabilities without naming their command more often than not. | `.claude/skills/closed-loop/SKILL.md` § CP-3 | **advice** — `.claude/skills/closed-loop/SKILL.md` § CP-3 Build. Nothing observes an enumeration that was never run: a search scoped too narrowly leaves output that looks exactly like a complete one, and the missing hits are missing from the evidence too |
| AI-17 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | Ship the near-duplicate detector (`04-projects/capability-parity/evidence/dupe-check.py`) into cadre as a test beside `documented_verbs_test.go`. It found four stale-beside-correction pairs that hand-checking missed individually. | `~/sdk/cadre/internal/cli/` | **control** — cadre `internal/cli/duplicate_paragraphs_test.go`, commit `32d863e1` |
| AI-18 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | The two-attempt fix budget is not the worker's to extend on the grounds that its method changed. On a third failure of the same criterion, stop and put it to the user: state the pattern, the proposed new method, let them spend the attempt. | `.claude/skills/closed-loop/SKILL.md` § fix budget | **advice** — `.claude/skills/closed-loop/SKILL.md` § CP-3v. The retry count *is* observable, but the defect is the judgment that this attempt differs in kind from the last — and a worker who believes that will record it as a first attempt at a new method |
| AI-19 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | After a `git checkout --` revert of an edit you are about to redo, diff against HEAD before continuing. A revert you believe happened is not a revert you observed. | working practice | **advice** — `.claude/skills/closed-loop/SKILL.md` § After a revert. A hook could diff after every `git checkout --`; COG ships no hook infrastructure. A cost argument, like AI-7, and it closes with AI-7 if hooks ever arrive |
| AI-20 | [2026-09-01 capability-parity](retro/2026-09-01-capability-parity.md) | A background command querying one repository must not resolve its arguments from the session's working directory. Pass `-R <owner/repo>` and an explicit sha. | working practice | **advice** — `CLAUDE.md` § Background commands. Same limitation as AI-19: a `PreToolUse` hook could inspect the command string, and there is no hook infrastructure to put it in |
| AI-21 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | Build the row-versus-reality cross-check. **Half built:** `citation-lint.sh` resolves cited commits and vault paths, falsified four ways, wired into the north-star gate. **The half that motivated the item is not built and is not cheap** — of the six defects in P2/P3 this would have caught none, because every one was a wrong claim about an artifact that existed. That semantic half is carried as AI-25. | `.claude/lib/citation-lint.sh` | **control** — `.claude/lib/citation-lint.sh`, commit `760dab3` |
| AI-22 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | `spec-lint.sh`'s verified-skip trusts the literal word `verified` with no cross-check against an evidence file. | `.claude/lib/spec-lint.sh` | **control — unbuilt** |
| AI-23 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | AI-13 guards a discarded path; the incident that named it turned on an unlogged version. Nothing covers the version case. | cadre | **control — unbuilt** |
| AI-24 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | When a check and the document teaching people to satisfy it are both authored, test the pair: write the artifact the document instructs, run the check on it. | `.claude/skills/closed-loop/SKILL.md` § CP-3v | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § CP-3v. No artifact exists at authoring time to compare against: the defect is that two artifacts were never brought together, which nothing can observe until someone does it |
| AI-25 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | The semantic half of AI-21. **Built for the reachable part:** `crossref-lint.sh` enforces three invariants between rows that name each other, and catches three of the six defects. **Not built:** whether a control guards what its row says — that needs reading a sentence against code. Carried as AI-26. | `.claude/lib/crossref-lint.sh` | **control** — `.claude/lib/crossref-lint.sh`, commit `4a2c9f2` |
| AI-26 | [2026-09-01 controls-not-advice](retro/2026-09-01-controls-not-advice.md) | Does a control guard what its row claims it guards? AI-13's row said it covered a version mismatch; it guards a discarded path. Reaching this means reading a claim against code. | cadre / harness | **control — unbuilt** |
| AI-19 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A tag with no release behind it. recall carries three (`v0.3.0`–`v0.3.2`). This is the check that would have caught P5's opening finding without a person looking. | each of the four repos | **control — unbuilt** — observable as `gh release list` vs `git tag` per repository; needs a home that runs it |
| AI-20 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A repository with no `LICENSE` of its own. Nothing in any of the four gates on it; recall's `go-licenses` checks *dependency* licences and would not notice. | each of the four repos | **control — unbuilt** — observable as a file-existence assert in each repo's own suite, gloop excepted by recorded decision |
| AI-21 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A release job that publishes from a commit whose validate run is red. `cli-v0.7.0` and `plugin-v0.24.0` went out that way. | `.github/workflows/release.yml` ×4 | **advice** — landed in `.claude/skills/ultragoal/SKILL.md` § the repositories must be green, with the `cli-v0.7.0` / `plugin-v0.24.0` incident attached. The release job *could* require the validate conclusion, so this is a cost argument, not an impossibility: it serialises every release behind a full matrix |
| AI-22 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A guard deleted by a later edit leaves no failing artifact. `TestDispatchHasNoUnlistedSpecialCase` was removed by a scripted rewrite and went unnoticed for five CP-3v rounds; only CP-4 found it. | gloop `internal/docguard/` | **control** — gloop `TestTheGuardSetOnlyGrows`, commit `1f37de4`. Names all 20 guards by hand: adding one requires editing the list, removing one fails |
| AI-23 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A hand-kept list beside a contract that could generate it. Three instances in P5 alone — the launcher's platform message, the wheel here-doc, `CADRE_KERNEL_REF` — each with a comment claiming it was kept in step. | cadre | **control** — `TestTheWheelPlatformsMatchTheContract` and `TestTheWorkflowKernelPinMatchesTheProviderPin`, commit `5c40d6ec`; the launcher's list now renders from `release.PlatformsFor` |
| AI-24 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A recorded exclusion carries its own expiry. `linux/arm64` was excluded because it "needs either a native arm64 runner or a cross toolchain"; the runner arrived and the reason lapsed silently. | working practice | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § CP-3 Build. A check cannot evaluate whether a prose reason still holds; what it *can* do is refuse a bare exclusion with no reason attached, which is why the reason was there to lapse |
| AI-25 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A guard that parses a document to check it will keep disagreeing with the document. Generate the expected block from the source of truth and assert the document contains it. | `.claude/skills/closed-loop/SKILL.md` § CP-3 | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § CP-3 Build with the three-round `--config` incident attached. The direction a guard points is a design decision, and a guard pointed the wrong way is a working guard that happens to be wrong — nothing observes that |
| AI-26 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | A fixture shared across subcommands, or a normaliser applied to output, can erase the signal the test exists to read. P6 produced an empty Honoured row that was nearly believed. | working practice | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § A fixture can destroy the evidence it exists to produce. At the point a check could look, an erased signal is indistinguishable from a legitimately empty result; what separates them is whether the *absence* was falsified |
| AI-27 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | `TestDispatchSDLC_UsesInTreeFallback` built its fake kernel at a path the production code read and the repository never had; test and code moved together, neither moved with the repository. | cadre `internal/cli/` | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § A fixture can destroy the evidence it exists to produce. A path a resolver treats as in-tree *could* be asserted to exist in the real checkout, but most are legitimately absent until something creates them, so the general check would be wrong more often than right. Cost argument; the specific fixture is fixed at `5c40d6ec` |
| AI-28 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | Verify the class of artifact the criterion names. AC-8 said the kernel has one release *home*; P2 checked the *workflow* that publishes to it and was wrong for six phases, until the north-star gate found six live releases with downloadable assets in the repo the criterion said had none. | `.claude/skills/closed-loop/SKILL.md` § CP-5 | **control — unbuilt** — partially observable: a CP-5 row citing a source file, workflow or config as its artifact, for a criterion whose text names a published or external thing, is a detectable mismatch. It would not have caught the same criterion's issue-body clause, where the wrong artifact and the right one are the same kind of thing. Rule landed meanwhile in `.claude/skills/closed-loop/SKILL.md` § CP-5 |
| AI-29 | [2026-09-02 production-readiness](retro/2026-09-02-production-readiness.md) | An HTTP 200 is evidence that a URL renders, not that the thing behind it exists. `releases/tag/<tag>` returns 200 for a bare git tag with no release; the north-star gate's own FAIL cited that 200 as proof a release was live, and it still returns 200 now the releases are deleted. Where an API answers the question directly, the HTML page does not. | `.claude/skills/closed-loop/SKILL.md` § CP-5 | **advice** — landed in `.claude/skills/closed-loop/SKILL.md` § CP-5 alongside AI-28. No check reaches it: the defect is choosing the weaker of two available observations, and the weaker one returns a plausible success |
