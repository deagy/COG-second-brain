# Harvest staging — 2026-09-02

## 19:50 — CP-7, production-readiness ultragoal

- type: process
- source: `04-projects/harness/retro/2026-09-02-production-readiness.md`
- text: A criterion's PASS row can be false for six phases when the check observes the machinery instead of the artifact. AC-8 said "the lifecycle kernel has one release home"; P2 verified that the publishing workflow no longer targeted the second one, which is a different claim — six releases were still live and downloadable. Nothing between P2 and the north-star gate would have caught it, because every intervening check was scoped to its own phase. The gate is what makes a multi-phase goal safe against its own early phases.
- proposed_home: `.claude/skills/closed-loop/SKILL.md` § CP-5 (landed) · AI-28

- type: process
- source: session
- text: An HTTP 200 is evidence a URL renders, not that the thing behind it exists. `github.com/<o>/<r>/releases/tag/<tag>` returns 200 for a bare git tag with no release. The north-star gate cited that 200 as proof a release was live; it still returns 200 now the releases are deleted. Where an API answers the question directly, prefer it — the HTML page answers a nearby question and returns a plausible success.
- proposed_home: `.claude/skills/closed-loop/SKILL.md` § CP-5 (landed) · AI-29

- type: process
- source: `04-projects/production-readiness/evidence/P6/CP-3v-escalation.md`
- text: A guard that parses a document to check it will keep disagreeing with the document. Three attempts to guard gloop's `--config` table each parsed the README and each failed for a new reason — one accused the README where the README was right, one searched the whole file so deleting a row left it green, one shared a fixture that overwrote its own evidence. The fourth generated the table from the binary and asserted the README contained it. The direction a guard points is a design decision, and three rounds failing on the same shape is the signal to invert it rather than refine it.
- proposed_home: `.claude/skills/closed-loop/SKILL.md` § CP-3 (landed) · AI-25

- type: process
- source: session
- text: A recorded exclusion carries its own expiry. `linux/arm64` was excluded from a platform list because it "needs either a native arm64 runner or a cross toolchain"; the runner arrived, the reason lapsed, and a clean-machine install found it by having no binary to fetch. No check can evaluate whether a prose reason still holds — but a bare entry in a list of unsupported things outlives its justification invisibly, whereas a stated reason can be read and found spent.
- proposed_home: `.claude/skills/closed-loop/SKILL.md` § CP-3 (landed) · AI-24

- type: skill-gap
- source: session
- text: A poll loop that treats an empty result as terminal will exit immediately. `gh run list --commit <sha>` returns nothing before the runs register, and a loop matching only on `in_progress`/`queued` reads that as finished. Poll the authority that distinguishes "not yet" from "done" — here `ci-status.sh`, which names PENDING explicitly.
- proposed_home: working practice; not filed as an AI (it is an instance of AI-29's shape rather than a new rule)

- type: fact
- source: `~/sdk/cadre-kernel/kernel/README.md`
- text: cadre-kernel's only README documents a Python distribution — `pip install`, `python3 -m agentic_sdlc`, `requirements-validation.txt` — and the repository contains zero `.py` files. It is a Go module publishing a Go binary. Found while fixing cadre's broken kernel install instructions, which link to it. No criterion in this goal covers cadre-kernel's documentation, so it was recorded rather than fixed.
- proposed_home: `04-projects/production-readiness/STATUS.md` § Found, and deliberately not fixed (landed)
