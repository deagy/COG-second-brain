# Harvest staging — 2026-08-28

Raw session capture for `/harvest`. Nothing here is durable knowledge until the human promotes it. Phase 2 (`/harvest promote`) curates these into promotion/fold/drop candidates.

## 21:07 — /harvest (manual end-of-session)

### 21:07 — Cline shim validation assertion
- type: process
- source: session (scripts/validate-agent-surface.sh edits)
- text: When adding a guard or assertion to a validation script, prove it actually fires by exercising the failure path, not just the happy path. For the new Cline shim assertion I temporarily moved CLINE.md aside, ran the script, confirmed the assertion reported the missing file, then restored it and re-ran to a clean pass. A happy-path pass alone would have given false confidence that the check catches a missing shim.
- proposed_home: fold into a Verify section for validation scripts, or a short process note in 05-knowledge/technical/

### 21:07 — Harvest promotion target does not exist
- type: skill-gap
- source: .claude/skills/harvest/SKILL.md:5, .claude/agents/harvest-curator.md:12,22
- text: The harvest skill and the harvest-curator agent both target a non-existent 05-knowledge/lizard/ directory ("stage for human promotion to 05-knowledge/lizard", "Existing 05-knowledge/lizard/ index for dedup", output paths like "05-knowledge/lizard/YYYY-MM-DD-<slug>.md"). This vault uses a distributed product/ + technical/ structure with no lizard folder. If /harvest promote runs, the curator would propose writing adoption notes to a path that does not exist and does not match the KB layout. Patch both files to target the actual structure (product/ or technical/ depending on topic) or create the promotion target.
- proposed_home: patch .claude/skills/harvest/SKILL.md and .claude/agents/harvest-curator.md

## Drop (already captured — not re-staged)
- `.cline/agents/` vs `.agents/agents/` one-hop redirect insight → already in 05-knowledge/product/cog-agent-clients.md Notes (marked resolved) and journaled 2026-08-28 20:58. Skipped per dedup.
