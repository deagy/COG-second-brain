---
type: harvest-staging
date: 2026-09-03
status: staged
source: cadre/COG integration — PR #1 review through PR #5 merge
---

# Harvest staging — 2026-09-03

Advisory. Nothing here is durable knowledge until promoted.

## 16:05 — /harvest (session: cadre-COG integration, four review passes)

- type: process
- source: session
- text: Three separate defects in this session were instructions written into a skill or plan doc and never executed — the Phase 7 approval fold (schema-invalid: `human_approvals` at the run-record root, which is `additionalProperties: false`), the Phase 7 re-entry fold (unfillable: TSV carried AC-ids where `$defs/invalidation` demands G-enums, and three required fields the TSV never captured), and the `worker-publisher` ledger check (`grep -P "\tG[89]\t" … | tail`, which matched on gate number alone and swallowed grep's exit status). All three passed a high-effort review as prose and failed on first execution. The existing CLAUDE.md § "Before you assert it, check it" covers assertions *to the user*; this is the adjacent case of an instruction written *for a future agent*, where the artifact exists but nobody ran it.
- proposed_home: `CLAUDE.md` § Before you assert it, check it — new fourth bullet

- type: skill-gap
- source: session
- text: A gate registry was built across four review passes mapping five external mutations to cadre gate ids, without anyone reading `00-inbox/MY-INTEGRATIONS.md`. Slack, Linear, Confluence and Notion are all **Disabled**; only GitHub and Discord are Active. So three of five rows gated services that do not run in this vault, and both live surfaces were absent. CLAUDE.md § Integration Preferences already says to check that file before using any external integration — the rule existed and was not reached, because the work presented as "governance design" rather than "using an integration".
- proposed_home: `CLAUDE.md` § Integration Preferences — extend the trigger from *using* an integration to *designing anything that names one*

- type: process
- source: session
- text: An approval is a moment-fact; a run-record is a document written at the end of a run. Writing the first into the second was unsatisfiable twice over — the gated skills are not harness entry points, so no run-record exists in an ordinary session, and inside `/closed-loop` the mutation is Phase 6 while the run-record is Phase 7. Naming that distinction collapsed five separate review findings into one fix, and the mechanism it called for already existed in `checkpoint.sh record_cp`: append at the moment, assemble the document later. Generalises to any "record X before Y" requirement where X is an event and Y is a summary.
- proposed_home: new note — `05-knowledge/technical/2026-09-03-moment-facts-and-end-of-run-documents.md`

- type: process
- source: session
- text: Change 3's approval gate was withdrawn because in a solo vault the agent supplies every field of its own approval — `record_approval` was a bash command the agent ran with arguments the agent chose, where the Publisher and the approver are the same person. It was also strictly weaker than the prompt beside it: two of four skills already stopped for an explicit yes (so the row added nothing), and the two that did not were unattended pipelines (so the row manufactured the appearance of a control). A control that looks like a control but passes unconditionally is worse than no control. The mechanisms that can attest a human decision are the ones the agent cannot execute — a permission prompt, and the user's own turn.
- proposed_home: new note — `05-knowledge/technical/2026-09-03-what-can-attest-a-human-decision.md`

- type: process
- source: session
- text: Four high-effort review passes ran across two PRs; findings clustered almost entirely in one change. Change 3 (mutation gates) produced findings in all four passes including every high-severity one; Changes 1 and 2 produced only mechanical defects that stayed closed. Two of the later findings were regressions introduced by fixing earlier ones. The cluster was the signal to split, not to patch again — the same reasoning that had already removed Change 4. Worth stating as a heuristic: when a component generates findings in every pass while its neighbours converge, the boundary is the finding.
- proposed_home: fold into `05-knowledge/consolidated/` (verification harness) or a new note if none fits

- type: fact
- source: session
- text: `gh pr view --json headRefOid,state` served stale data immediately after a successful push or merge on three separate occasions — once reporting `OPEN` with `mergeCommit: null` for a PR that `gh pr merge` simultaneously reported as already merged. `git ls-remote origin refs/heads/<branch>` and `git merge-base --is-ancestor` are the authoritative checks; the GitHub API view can lag by seconds to a minute.
- proposed_home: fold into `05-knowledge/technical/2026-09-01-github-actions-gotchas.md`

- type: correction
- source: session
- text: `skills/` at the COG repo root is a **generated** mirror of `.claude/skills/` (`scripts/build-agent-plugin.sh`: "skills/ is GENERATED — never edit it by hand"), validated by `scripts/validate-agent-surface.sh`. Change 3 hand-edited it, leaving `skills/publish-to-confluence/SKILL.md` carrying two contradictory gate paragraphs (a stale G7 and the correct G9) where `authority-gates.md` defines the publish as G9 and has no G7 at all. The stale text would have been silently destroyed on the next regeneration.
- proposed_home: already fixed in the code; fold the *rule* into `05-knowledge/technical/` if a COG-packaging note exists, else skip as repo-recorded

## 21:40 — /harvest (session continued: recall/cadre/gloop releases, loader build)

- type: fact
- source: session
- text: `go test` serves cached results, which silently voids mutation testing. During a mutation check on the conversation loader, a mutation appeared to survive; the run had actually printed `ok (cached)` and never executed. With `-count=1` the mutation genuinely survived, which then exposed a real test gap. Every mutation check must pass `-count=1`, and the two failure modes look identical from the outside: a mutation that survives because the test is weak, and one that "survives" because it never ran.
- proposed_home: fold into `05-knowledge/technical/2026-09-03-instructions-that-were-never-run.md`, or a new note on mutation-testing mechanics

- type: process
- source: session
- text: A surviving mutation is a question, not an answer. Twice in one session a surviving mutation meant something other than "the test is weak" — once it was the test cache, once it was a real but *different* gap than assumed (the "wrong mapping" case failed on the Turns guard before the Text check ever ran, so half of matching was untested). Investigating why a mutation survived produced a better test both times; concluding "weak test" and rewriting blindly would have produced neither.
- proposed_home: fold into `05-knowledge/technical/2026-09-03-instructions-that-were-never-run.md`

- type: correction
- source: session
- text: A doc comment describes an API's contract to its consumers; it does not tell you whether *your* consumer uses it. `loader.ForExtension`'s comment says "callers can register their own via DirectoryLoader.Loaders", and I concluded twice in a braindump that this was the seam for a chat-export loader. `loaderForPath` in the CLI passes `nil` for that field and routes single files around `DirectoryLoader` entirely. The quote was accurate; the inference was not. The missing step was one grep for the field's callers.
- proposed_home: fold into `05-knowledge/technical/2026-09-03-instructions-that-were-never-run.md`

- type: fact
- source: session
- text: Release mechanisms differed in all three repos touched, and assuming any one would have broken the others. `recall`: a manual `Tag release` workflow_dispatch that pushes the tag *and calls* `release.yml` directly, because a ref pushed with the workflow's own GITHUB_TOKEN raises no further workflow — v0.3.0 and v0.3.1 were both tagged and published nothing before that was fixed. `cadre`: no tag workflow at all; `release.yml` fires automatically when a version bump lands on `main`, and the version lives in three places (`VERSION`, `CADRE_CLI_VERSION` in `plugin/bin/cadre`, the CHANGELOG heading) — the third was only findable by reading the previous release commit. `gloop`: no release workflow, mixed annotated/lightweight tags, and no GitHub Releases at all.
- proposed_home: new note — `05-knowledge/technical/2026-09-04-release-mechanisms-differ-per-repo.md`

- type: fact
- source: session
- text: A merged PR can be left un-merged in GitHub's own records. PR #6 in COG-second-brain had its merge commit on `main` with GitHub's own "Merge pull request #6" message, while the API reported `state: open`, `merged: false`, no merge event, and a `merge_commit_sha` pointing at the *predicted* merge commit rather than the real one. Re-running `gh pr merge` then returns "Pull Request is not mergeable", because the head is already contained in the base — so the PR can never be transitioned to merged through the API and has to be closed with the facts recorded in a comment. `GET /compare/base...head` returning `ahead_by=0` is the check that settles it.
- proposed_home: fold into `05-knowledge/technical/2026-09-01-github-actions-gotchas.md`

- type: skill-gap
- source: session
- text: Before releasing a library, enumerate its importers from disk rather than from memory. Searching every `go.mod` under `~/sdk` found `gloop` pinned at recall `v0.1.0` — four minors behind, crossing two breaking changes — which nobody would have surfaced by asking "what depends on recall?". The follow-on discipline: check each breaking change against the importer's *actual* usage before bumping, and take a baseline test count **before** the bump so the after-count means something. gloop turned out unaffected by both, but only because the paths those changes moved were ones it did not use.
- proposed_home: new note — `05-knowledge/technical/2026-09-04-bumping-a-library-across-breaking-changes.md`

- type: skill-gap
- source: session
- text: `recall`'s `release.yml` never touches the CHANGELOG, so `## [Unreleased]` is promoted by hand or not at all — and it was not. Six released versions (v0.3.2 through v0.3.6) have no CHANGELOG section, and v0.4.0's content was still sitting under Unreleased after that release published. The structural fix is to teach the release workflow to promote the section; the manual alternative recurs every release. Recorded rather than fixed: reconstructing six missing sections needs someone who knows what shipped in each.
- proposed_home: `~/sdk/recall` issue, not COG knowledge — flag to the user

## 21:50 — /retro (CP-7 for the cadre/COG integration and recall releases)

> Heading corrected 22:00: the original read `22:15`, a time I wrote without running `date`. It had not happened yet.

- type: process
- source: `04-projects/harness/retro/2026-09-03-cadre-cog-integration-and-recall-releases.md`
- text: The session shipped to three repositories and cut three releases with no lane, no spec and no gate — `verification_harness: off`, zero loop-ledger rows, no run directory. What substituted was four review passes, mutation checks and artifact verification, every one of them chosen rather than enforced. Recorded in `WORKFLOW.md` § Scope as a question to answer rather than a defect to fix: either the harness's trigger is too narrow, or its scope is genuinely narrower than its length suggests.
- proposed_home: landed in `WORKFLOW.md` § Scope (AI-44)

- type: skill-gap
- source: session
- text: `backlog-lint.sh` refused four rows whose disposition was `advice` but which cited only the retro and a knowledge note as where the rule landed. It was right: the rule is that advice lands somewhere read at session start — CLAUDE.md, WORKFLOW.md or a skill body — and a note in `05-knowledge/` is not that. The lint forced three rules to actually be written into CLAUDE.md rather than merely recorded, which is the difference the disposition system exists to enforce.
- proposed_home: no new home — this is the control working as designed, worth noting as evidence it does

## 22:00 — /harvest (third pass; little new, and that is the finding)

- type: correction
- source: `04-projects/harness/harvest/staging-2026-09-03.md`
- text: I stamped the retro's staging section `22:15` without running `date`. The real time was before 22:00 — the timestamp was in the future when I wrote it. The braindump skill states the rule explicitly ("NEVER guess or fabricate the time — always use the value returned by the `date` command") and harvest inherits the same `<HH:MM>` convention without restating it. Corrected to 21:50 with a note in place. Small, but it is a fabricated fact in an evidence file, in a session whose main lesson was not asserting what was never checked.
- proposed_home: `.claude/skills/harvest/SKILL.md` — Phase 1 should carry the same "get the timestamp with `date`" instruction the braindump skill does

- type: process
- source: session
- text: The harvest curator disagreed with the lead's proposals in both batches, and was right both times. Batch 1 rejected a proposed `CLAUDE.md` patch because it contradicted the target section's own stated rationale. Batch 2 refused all three proposed folds (the target note is scoped to prose-review-vs-execution; a test runner serving a cached result is a different failure), merged two proposed notes on a shared cause the lead had stated twice without noticing, and caught a date error. The pattern: the author of a batch is the worst-placed reader of whether it is redundant with what they already wrote, because the connections that made them write it are the same ones that make it feel distinct.
- proposed_home: fold into `05-knowledge/technical/2026-09-03-findings-cluster-is-the-boundary.md`, or a new note on why the curator is a separate agent

- type: process
- source: session
- text: A third `/harvest` in one session found two items where the first found seven and the second seven. Most of what happened since the second pass was the harvest and retro machinery itself, and the retro's Phase 5 had already staged its own findings. Recorded because the useful signal is the count, not the content: when a harvest returns almost nothing, the session has been captured, and running it again to feel thorough would manufacture entries rather than find them.
- proposed_home: no durable home — this is an observation about pacing, not knowledge
