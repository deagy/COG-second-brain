# CP-4 integration verification — capability-parity ultragoal (retrospective)

Scope: cross-phase wiring only. Component-level CP-3v and per-criterion north-star gates already passed and are not re-verified here. Code checked at `deagy/cadre` `b534fb27`, branch `main`. Tree left clean (verified `git status` empty after all probes; all injected mutations reverted).

## VERDICT: FAIL:fixable

## INTEGRATION_CLAIMS_CHECKED: 14

## EVIDENCE

EVIDENCE AC-7 | CP-4 | FAIL | The drift guard's scan roots are `roster/` and `.agents/skills/` (plus `CHANGELOG.md`'s `[Unreleased]` section) — `internal/cli/documented_verbs_test.go` `roots := []string{filepath.Join(repo,"roster"), filepath.Join(repo,".agents","skills")}`. Repo-root files are never walked. Empirically confirmed: appended `` `cadre knowledge phantomverb999` `` to `RELEASE_NOTES_PHASE4.md` (repo root) and `TestEveryDocumentedKnowledgeVerbIsAnswerable` still PASSed; the same line appended to `roster/knowledge-store/README.md` made the same test FAIL correctly, naming the file and line. Reverted both edits (`git status` empty). | `/home/deagy/sdk/cadre/internal/cli/documented_verbs_test.go` (roots), live test runs above

EVIDENCE AC-1/AC-7 | CP-4 | PASS-with-caveat | Every verb the repo-root `RELEASE_NOTES_PHASE4.md`/`PHASE4_ROADMAP.md` currently name (`init`, `search`, `ingest`, `delete`, `stats`) already resolves through `retiredVerbs`/`liveKnowledgeVerbs` in `internal/cli/knowledge.go`, so today's content would not trip the guard even if scanned. One multi-line backtick span ("the `` `cadre knowledge\ndelete` `` command") also silently evades the single-line `codeSpanPattern` regardless of scan root. No live drift exists today; the exposure is forward-looking (item above). | `RELEASE_NOTES_PHASE4.md`, `PHASE4_ROADMAP.md`, `internal/cli/knowledge.go:26-100`

EVIDENCE AC-2/AC-3/AC-4 | CP-4 | PASS | `AnswerableKnowledgeVerbs()` (`retiredVerbs`, `pythonEraVerbs`, `liveKnowledgeVerbs`, `knowledgeStagedSubcommands`) agrees with every verb-claim read in `roster/knowledge-store/{README,SECURITY,AGENT}.md`, `roster/workflows/knowledge-ingestion.md`, and the top-level `context` subcommand (`promote`/`get`) named in `AGENT.md:15` — confirmed live in `internal/cli/context.go`/`dispatcher.go:105`. No verb found where code and prose disagree on live/retired/absent status. | `internal/cli/knowledge.go`, `internal/cli/knowledge_staged.go:34-43`, `internal/cli/context.go`, `internal/cli/dispatcher.go:105`, `roster/knowledge-store/{README,SECURITY,AGENT}.md`, `roster/workflows/knowledge-ingestion.md`

EVIDENCE AC-2 | CP-4 | PASS | P3's "out of scope" cross-phase corrections (`.agents/skills/run-agent-orchestration/references/dispatch-contract.md`, `.agents/skills/knowledge-ingestion/SKILL.md`) still state the deletion/retention gaps and the `context` retirement accurately at `b534fb27` — read directly, not via P3's summary. | `.agents/skills/run-agent-orchestration/references/dispatch-contract.md:25`, `.agents/skills/knowledge-ingestion/SKILL.md:37`

EVIDENCE AC-3/AC-4 | CP-4 | PASS | `roster/knowledge-store/DESIGN-NOTES-deletion-and-retention.md` opens "Nothing described here is currently implemented" / "Kept verbatim from SECURITY.md... only the claim they make about the world has been corrected," and its content (retired verbs `retention-report`/`delete-ingested`/`deletion-evidence`, recall's Go API deleting by id, no CLI delete) agrees exactly with P3's absence declaration in `SECURITY.md`/`AGENT.md`. Banner intact, no drift between P2's preserved input and P3's declared-absent output. | `roster/knowledge-store/DESIGN-NOTES-deletion-and-retention.md:1-21`, `roster/knowledge-store/SECURITY.md:38-46`

EVIDENCE AC-5 | CP-4 | PASS | All four line-number citations in `evidence/P4/CP-5-acceptance-AC-5.md` (`internal/cli/knowledge_staged.go:585`, `internal/knowledge/staged_ingest.go:293`, `internal/knowledge/staged_store.go:212`, `internal/knowledge/staged_store.go:559`) still point at the exact cited constructs (`StagedRecordIsSelfApproved` call in `import-staged`, `stagedIngestRefusal`, the predicate itself, `DispositionStagedRecord`'s inline check) at `b534fb27`. `git log 1e317729..b534fb27 -- <those 3 files>` returns no commits — the evidence's commit skew (`1e317729` vs current `b534fb27`) touched none of the cited files, so the line numbers are exact, not coincidentally stable. | `internal/cli/knowledge_staged.go:575-595`, `internal/knowledge/staged_ingest.go:283-300`, `internal/knowledge/staged_store.go:200-222,550-565`, `git log` output

EVIDENCE global | CP-4 | PASS | Generated trees (`provider/`, `plugin/`, `cline-plugins/`) reflect the final post-P4 state and regeneration is idempotent: ran `go build ./cmd/cadre && generate-role-metadata && generate-plugin --output plugin && port-cline-agents --root cline-plugins --source plugin`; `git status --porcelain` was empty afterward (0 lines). No stale generated copy of a corrected document exists. | live command output, `git status` clean

EVIDENCE AC-7 wiring | CP-4 | PASS (in-scope only) | Regression-tested that AC-7's guard genuinely protects AC-2's in-scope corrections: injecting a phantom verb into `roster/knowledge-store/README.md` makes `TestEveryDocumentedKnowledgeVerbIsAnswerable` fail, naming the exact file/line; reverting restores green. The guard is real and mechanical for everything under `roster/` and `.agents/skills/`. | live test run (FAIL then PASS after revert), `roster/knowledge-store/README.md` (reverted, `git status` empty)

## FAILURES

- AC-7 (P1) / AC-2,AC-3,AC-4 (P2,P3) wiring gap | The drift guard built in P1 scans only `roster/` and `.agents/skills/` (plus `CHANGELOG.md [Unreleased]`). P3's own ledger records that its worst-found defects — `RELEASE_NOTES_PHASE4.md` and `PHASE4_ROADMAP.md` at the repository root — were *outside* that scope ("A phase scoped to the knowledge-store documents would never have opened any of them"), and both north-star gates (`evidence/northstar-gate.md:58`, `evidence/northstar-gate-2.md:52`) manually re-read those two files and confirmed the banners are currently correct — but neither gate checked, and no test asserts, that the *mechanism* protecting AC-2's corrections reaches them. It doesn't: reintroducing a phantom `cadre knowledge` verb into either file today passes CI silently. Observed: guard PASSes with an injected phantom verb at repo root; expected (per AC-7's own claim "the parity property is enforced, not restored," and per spec's north-star "every capability the roster's governance documents describe exists, or the documents say it does not") is a guard that fails there too, since these are governance documents the goal itself treated as in-scope once discovered. Fix is scoped and cheap: add the two files (or the repo root, excluding archival/superseded conventions already established for `roster/orchestration/runs/`) to `documented_verbs_test.go`'s `roots`/scan list, plus a regression test asserting it. No code or evidence needs to be reopened beyond that file.
