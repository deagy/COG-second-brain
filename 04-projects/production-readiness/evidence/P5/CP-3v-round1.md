## VERDICT: FAIL:fixable
## LANE: full
## CLAIMS_CHECKED: 15

## EVIDENCE
EVIDENCE AC-6 | CP-3v | PASS | cadre-kernel: HEAD d4fb0894 == tag v0.14.4, 0 commits | `git log --oneline v0.14.4..HEAD` in ~/sdk/cadre-kernel (empty)
EVIDENCE AC-6 | CP-3v | PASS | recall: HEAD 3bef2354 == tag v0.3.3, 0 commits | `git log --oneline v0.3.3..HEAD` in ~/sdk/recall (empty)
EVIDENCE AC-6 | CP-3v | PASS | gloop: 45 commits ahead of v0.2.0, but the stated reason ("main is what you install... a tag that produces no release is indistinguishable from one that failed to") is written in README.md itself, not only in the vault | ~/sdk/gloop/README.md lines 7-9
EVIDENCE AC-6 | CP-3v | FAIL | cadre: HEAD is now ce7b1e01 ("fix(sdlc): find the kernel a plugin install actually produces"), 1 commit ahead of the latest release tag cli-v0.7.2 (37afe44a is plugin-v0.24.2, e7d9a25c is cli-v0.7.2). No stated reason for this gap is written anywhere in the repository — it is an unreleased commit sitting on main | `git log --oneline cli-v0.7.2..HEAD` = ce7b1e01 in ~/sdk/cadre
EVIDENCE AC-6 | CP-3v | PASS | kernel v0.14.3 SHA256SUMS uses `sha256sum ./*` form (`./agentic-sdlc-v0.14.3-linux-amd64.tar.gz`) — confirmed does NOT match shim's `grep " $ARCHIVE$"`; v0.14.4 uses bare names and does match | downloaded SHA256SUMS + local grep test, /tmp/claude-1000/verify-p5/kernel-v0.14.3, kernel-v0.14.4
EVIDENCE AC-6 | CP-3v | PASS | kernel v0.14.2 archive carries no LICENSE; v0.14.3 and v0.14.4 both do | `tar tzf agentic-sdlc-v0.14.2-linux-amd64.tar.gz` = `agentic-sdlc` only
EVIDENCE AC-6 | CP-3v | PASS | kernel release.yml assertions are real, not decorative — mutation-tested 5 ways locally (LICENSE present/absent/docs-only; SHA256SUMS bare vs `./`-prefixed), all behave exactly as the build record claims | /tmp/claude-1000/verify-p5/mutation-test, exit codes recorded inline
EVIDENCE AC-6 | CP-3v | PASS | recall tag.yml→release.yml wiring is real: v0.3.2's "Tag release" run shows conclusion=failure (the loud red run from the missing cadre-checkout steps), v0.3.3's run shows success and 13 assets published, `release.yml` refuses any TAG not matching `v[0-9]+.[0-9]+.[0-9]+` including the workflow_dispatch/workflow_call paths | `gh run list -R deagy/recall`, `gh release view v0.3.3`
EVIDENCE AC-6 | CP-3v | PASS | recall-0.3.3-linux-arm64 downloaded and run, reports `recall version v0.3.3` exactly as claimed | /tmp/claude-1000/verify-p5/recall-v0.3.3
EVIDENCE AC-7 | CP-3v | PASS | cadre's platform contract agrees across all four sources: `internal/release/platforms.go` PlatformsFor(ProgramCLI), the `cli` matrix in release.yml (ubuntu-latest/ubuntu-24.04-arm/macos-latest/windows-latest, no darwin/amd64), Makefile's `cross-build` (4 CLI targets, no darwin-amd64), and DISTRIBUTION.md line 40 all list exactly linux/amd64, linux/arm64, darwin/arm64, windows/amd64. Confirmed the launcher's `publishedCLIPlatforms()` renders from `PlatformsFor(ProgramCLI)` by running it in an isolated scratch clone: output was exactly `linux/amd64, linux/arm64, darwin/arm64 and windows/amd64` (not the old, wrong `linux/arm64, darwin/amd64` message) | /tmp/claude-1000/verify-p5/cadre-scratch/internal/release/zzverify_test.go (scratch-only, discarded)
EVIDENCE AC-7 | CP-3v | PASS | `TestTheWorkflowKernelPinMatchesTheProviderPin` genuinely fails under both named mutations: pin mismatch (v0.14.3 vs v0.14.4) and variable rename (CADRE_KERNEL_REF→CADRE_KERNEL_PIN) | mutation test in /tmp/claude-1000/verify-p5/cadre-scratch (git checkout -- restored after each)
EVIDENCE AC-7 | CP-3v | PASS | cadre HEAD e7d9a25c (T-06 commit, the one that fixed the wheel-count divergence) was confirmed against GitHub Actions history: `f05f0643` shows "Release push completed failure" matching the "expected 4 wheels, found 3" claim, and `6af4047c` (tagged cli-v0.7.0/plugin-v0.24.0) shows Release=success but validate=failure, exactly matching the T-03 claim about a red validate run publishing anyway | `gh run list -R deagy/cadre`, `git tag --points-at 6af4047c`
EVIDENCE AC-7 | CP-3v | PASS | gloop's `make install` is real: ran it in a fresh scratch clone with GOBIN redirected, produced a working `gloop` binary that prints correct usage. No other stale install claims (go get/go install/pkg.go.dev/badges) remain anywhere in the repo's markdown outside the README's own historical negation paragraph | /tmp/claude-1000/verify-p5/gloop-scratch, repo-wide grep
EVIDENCE AC-7 | CP-3v | FAIL | The T-07 fix (committed as ce7b1e01, not yet released) does not close the gap it claims to close. Reproduced install.sh's own documented sequence (pre-warm kernel via `plugin/plugins/lifecycle/bin/agentic-sdlc --version`, then run `cadre sdlc --version`) end to end in a scratch clone with Go removed from PATH, matching a genuinely toolchain-less container: the kernel downloads and caches correctly, but `./bin/cadre sdlc --version` still fails with `cadre: install Agentic SDLC v0.14.4+ from https://github.com/deagy/cadre-kernel`, exit 1 — the identical failure the build record says T-07 fixed. Root cause: `bin/cadre` (Go-present path) exports `CADRE_REPO_ROOT=$REPO_ROOT` (the full checkout), which does contain `plugin/plugins/lifecycle/bin/agentic-sdlc`, so that path now works (confirmed separately, exit 0, prints "0.14.4"). But the no-Go fallback, `plugin/bin/cadre`, execs the downloaded binary with `CADRE_REPO_ROOT="$SUITE_ROOT"` ($PLUGIN_ROOT/suite — a docs/roster tree with no `agentic-sdlc` anywhere under it, confirmed by `find`). Even `$PLUGIN_ROOT` itself wouldn't fix it: in a real installed plugin cache the shim lives at `PLUGIN_ROOT/plugins/lifecycle/bin/agentic-sdlc` (no extra `plugin/` segment — confirmed against the actual installed cache at ~/.claude/plugins/cache/cadre-team/cadre/0.23.3/plugins/lifecycle/bin/agentic-sdlc), while `PackagedKernelShim(repoRoot)` unconditionally joins `repoRoot/plugin/plugins/lifecycle/bin/agentic-sdlc`. Neither SUITE_ROOT nor PLUGIN_ROOT satisfies that join. The new tests (`TestPATHStillBeatsThePackagedShim`, the kernel_probe_test.go additions) only exercise `PackagedKernelShim` as a Go function with a hand-supplied repoRoot — none exercise what value the shell launcher actually passes on the no-Go path, which is exactly why this gap wasn't caught | /tmp/claude-1000/verify-p5/cadre-scratch2 (HEAD ce7b1e01), reproduction commands and outputs recorded inline above
EVIDENCE AC-7 | CP-3v | FAIL | Independent of the above, AC-7 currently fails simply because the T-07 fix is unreleased: `cli-v0.7.2` (the latest published cadre release) predates ce7b1e01 by one commit, so a clean machine installing from currently-published artifacts hits the pre-fix bug regardless of whether the fix itself is complete | `git log --oneline cli-v0.7.2..HEAD` in ~/sdk/cadre = ce7b1e01

## FAILURES
- AC-6 | "zero commits between latest release tag and HEAD, or a stated reason" | cadre's HEAD (ce7b1e01) is 1 commit ahead of cli-v0.7.2 with no stated reason recorded in the repository; this is live, uncommitted-turned-committed work landing during this verification pass, not a documented exception
- AC-7 | "installing from the artifacts P5 publishes produces a cadre that answers cadre sdlc --version" | Reproduced directly: even after the T-07 fix lands on main, the no-Go packaged-launcher install path (`bin/cadre` → `plugin/bin/cadre`, the path a toolchain-less container takes) still cannot resolve a kernel it has already downloaded and cached, and fails with the exact pre-fix error message. The fix only closes the gap for the Go-toolchain build path
- AC-9 (build-record overstatement) | T-07's narration ("PackagedKernelShim is now the shared last resort for the first two... TestPATHStillBeatsThePackagedShim holds that") reads as complete | The claim is true only for `DispatchSDLC`/`ResolveKernel` invoked with a source-checkout repoRoot. It is not true for the packaged/no-Go install path, which is the scenario T-05 and T-06 were specifically built to catch (a container with no Go toolchain) and the one AC-7's own wording ("Run in a container or equivalent, not a developer machine") most directly targets

## FIX_HINTS
- ~/sdk/cadre plugin/bin/cadre:298 (and :285) — the no-Go launcher execs the downloaded CLI binary with `CADRE_REPO_ROOT="$SUITE_ROOT"`. Either give `PackagedKernelShim` a second candidate that matches the packaged-plugin layout (`<root>/plugins/lifecycle/bin/agentic-sdlc`, no `plugin/` prefix) and pass `$PLUGIN_ROOT` for that purpose, or add a dedicated env var the launcher can set (e.g. `CADRE_PACKAGED_KERNEL_SHIM=$PLUGIN_ROOT/plugins/lifecycle/bin/agentic-sdlc`) so the two installation layouts don't have to share one path convention that only fits one of them
- ~/sdk/cadre internal/orchestration/kernel_probe.go:PackagedKernelShim — add a test that drives the actual no-Go launcher script (`plugin/bin/cadre`) end to end with Go absent from PATH, not just the Go function in isolation, so this class of gap is caught before publish rather than by another container run
- ~/sdk/cadre — cut and publish a release for HEAD (ce7b1e01, or whatever HEAD is once the above is fixed) so AC-6 and AC-7 are checked against what a clean machine can actually install, not against work still sitting on main
