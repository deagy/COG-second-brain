## VERDICT: PASS
## LANE: full
## CLAIMS_CHECKED: 17

## EVIDENCE
EVIDENCE AC-7 | CP-3v | PASS | Reproduced round 1's own failing sequence at HEAD 5c40d6ec (== cli-v0.7.5/plugin-v0.24.5, both released): fresh clone, PATH stripped to `/usr/bin:/bin:/usr/local/bin` (no `go` resolvable), pre-warmed the kernel via `./plugin/plugins/lifecycle/bin/agentic-sdlc --version` (exit 0, `0.14.4`), then ran `./bin/cadre sdlc --version` — prints `0.14.4`, exit 0. No `.cadre-build-cache` was created (confirms the Go path was never used) and `~/.cache/cadre/cadre-v0.7.5-linux-arm64` was the binary actually downloaded and exec'd, i.e. the real no-Go fallback path round 1 broke on | /tmp/claude-1000/verify-p5-r2/cadre-scratch (scratch clone, discarded), commands and output recorded inline above
EVIDENCE AC-7 | CP-3v | PASS | Second AC-7 command in the same session: `./bin/cadre knowledge search` (no query) → `cadre knowledge search: query is required`, exit 2, matching the criterion's expected shape and CP-3-build.md's own container transcript | same scratch clone
EVIDENCE AC-7 | CP-3v | PASS | Container transcript `evidence/P5/ac7-clean-machine.log` (2026-09-02T16:55Z, aarch64, Go absent, nothing on `~/sdk`) independently reproduces the same result end to end through the real `install.sh` + Claude plugin install: `cadre sdlc --version` → `0.14.4` exit 0; `cadre knowledge search` → exit 2 with a query-required message; `cadre doctor` reports `lifecycle kernel: 0.14.4 via packaged plugin` at `/root/.cadre/dist/plugin/plugins/lifecycle/bin/agentic-sdlc`, which exists on disk per the log's own `find` listing | evidence/P5/ac7-clean-machine.log
EVIDENCE AC-6 | CP-3v | PASS | cadre: `cli-v0.7.5` and `plugin-v0.24.5` are annotated tags whose dereferenced commit (`git rev-parse cli-v0.7.5^{commit}`) equals HEAD (5c40d6ec); `git log --oneline cli-v0.7.5^{commit}..HEAD` is empty. (Note: `git rev-parse cli-v0.7.5` alone returns the tag *object* SHA, not the commit — dereferencing with `^{commit}` is required or this reads as a false gap) | ~/sdk/cadre, commands recorded inline
EVIDENCE AC-6 | CP-3v | PASS | cadre-kernel: HEAD d4fb0894 == latest release `v0.14.4`'s commit, 0 ahead. recall: HEAD 3bef2354 == latest release `v0.3.3`'s commit, 0 ahead. Measured against `gh release list`, not `git tag` — recall still carries three tags (v0.3.0-v0.3.2) with no release behind any of them, confirming the ledger's tag-vs-release distinction still holds | ~/sdk/cadre-kernel, ~/sdk/recall
EVIDENCE AC-6 | CP-3v | PASS | gloop: no release exists (`gh release list` empty), and the reason is recorded in the repository itself, not only in the vault: README.md lines 7-9 state `main` is what you install, tags are markers, and a tag producing no release is indistinguishable from one that failed to | ~/sdk/gloop/README.md
EVIDENCE | CP-3v | PASS | Four kernel resolvers agree and none reads a dead path. `DispatchSDLC` (internal/cli/sdlc.go): config `agentic_sdlc.bin_path` → `PackagedKernelShim(repoRoot)`. `ResolveKernel` (internal/orchestration/kernel_probe.go, used by `cadre doctor`/`cadre select`): explicit → PATH → `PackagedKernelShim(repoRoot)`. The packaged launcher's shell `sdlc` branch (plugin/bin/cadre:282-299): `AGENTIC_SDLC_BIN` → configured `bin_path` → `PLUGIN_ROOT/plugins/lifecycle/bin/agentic-sdlc` directly. `agentic_sdlc.bin_path`'s `ComputedDefault` (internal/config/registry.go:105): `lookupWhich("agentic-sdlc")`, PATH-only by design — an unresolved default correctly falls through to `PackagedKernelShim` in the two Go call sites rather than claiming a dead path itself. `PackagedKernelShim`'s two candidates (`repoRoot/plugin/plugins/lifecycle/bin/agentic-sdlc` for a checkout, `filepath.Dir(repoRoot)/plugins/lifecycle/bin/agentic-sdlc` for `<package>/suite`) were traced against both the dist-checkout layout the container log shows and the real installed Claude plugin cache at `~/.claude/plugins/cache/cadre-team/cadre/0.23.3/plugins/lifecycle/bin/agentic-sdlc` — both resolve correctly under the second candidate | internal/orchestration/kernel_probe.go, internal/cli/sdlc.go, plugin/bin/cadre:282-309, internal/config/registry.go:105-111, ~/.claude/plugins/cache/cadre-team/cadre/0.23.3
EVIDENCE | CP-3v | PASS | `TestTheGeneratedLauncherFallsBackToTheLifecycleShim` fails for the stated reason under two mutations in the scratch clone: (1) deleting the `[ -x "$PLUGIN_ROOT/plugins/lifecycle/bin/agentic-sdlc" ]` fallback block → fails with "does not fall back to the lifecycle shim"; (2) reordering the fallback ahead of the `AGENTIC_SDLC_BIN` read → fails with "the shim fallback is checked before AGENTIC_SDLC_BIN". Neither mutation broke compilation | /tmp/claude-1000/verify-p5-r2/cadre-scratch, plugin/bin/cadre restored after each
EVIDENCE | CP-3v | PASS | `TestTheLauncherActuallyExecsTheLifecycleShim` (which actually execs `plugin/bin/cadre` under `sh` with a scrubbed PATH and a stub shim) fails under mutation (1) above with the exact pre-fix message reproduced: `cadre: install Agentic SDLC v0.14.4+ from https://github.com/deagy/cadre-kernel`, exit 1 — this is the launcher-execution guard round 1 asked for, and it is real | same scratch clone
EVIDENCE | CP-3v | PASS | `TestPATHStillBeatsThePackagedShim` fails for the stated reason ("the packaged shim overrode an installed kernel") when `ResolveKernel`'s switch is mutated to check `PackagedKernelShim` before `lookPath`. This directly answers "does the shim fallback ever override an operator's own kernel?" — no, and the guard catches it if it ever did | /tmp/claude-1000/verify-p5-r2/cadre-scratch, internal/orchestration/kernel_probe.go restored after
EVIDENCE | CP-3v | PASS | `TestThePackagedShimIsFoundWhenNothingIsOnPath`, `TestThePackagedSuiteLayoutResolvesToo`, `TestDoctorAndSdlcResolveTheSameKernel` all fail for the stated reason when `PackagedKernelShim` is mutated to unconditionally return `("", false)` | same scratch clone
EVIDENCE | CP-3v | PASS | `TestTheWheelPlatformsMatchTheContract` fails for the stated reason under three mutations to `.github/workflows/release.yml`'s `PLATFORMS` here-doc: missing row (`linux/arm64` → `linux/arm64XXX`) reports both "publishes X but builds no wheel" and "builds a wheel for Y which the CLI does not publish"; extra row (`linux riscv64` added) reports the same "no binary to put in it" message; renaming both here-doc markers (`PLATFORMS`→`PLATFORMS2`) fails with "no PLATFORMS here-doc... the guard now passes over a list nothing checks" rather than passing silently | /tmp/claude-1000/verify-p5-r2/cadre-scratch, release.yml restored after each
EVIDENCE | CP-3v | PASS | `cadre doctor`'s kernel resolution (internal/cli/doctor.go:70-77) uses `platform.FindInstallationRoot()`, not `platform.RepoRoot()`, matching T-09's claim exactly — comment at doctor.go:70-76 states the reason verbatim and matches the round-1-independent finding | internal/cli/doctor.go
EVIDENCE | CP-3v | PASS | `TestDispatchSDLC_UsesInTreeFallback`'s fixture now builds its fake kernel at `plugin/plugins/lifecycle/bin/agentic-sdlc`, not the stale `<repoRoot>/bin/agentic-sdlc` path CP-3-build.md says it used to; comment at internal/cli/sdlc_test.go:74-78 states this was the reason no suite caught the original defect | internal/cli/sdlc_test.go:69-90
EVIDENCE | CP-3v | PASS | Every test CP-3-build.md names for T-01 through T-09 exists in the tree exactly once (`TestTheWorkflowKernelPinMatchesTheProviderPin`, `TestEveryDocumentedKnowledgeVerbIsAnswerable`, `TestHardcodedReleaseTagsAreConfinedToHistoricalRecords`, `TestThisRepositorysChangelogHasAnEntryForItsCurrentVersion`, `TestEveryRunnerLabelIsReviewed`, plus the five named in the brief) — no fabricated or stale test name in the build record | grep across ~/sdk/cadre
EVIDENCE | CP-3v | PASS | `ubuntu-24.04-arm` (T-06's added runner) is both in release.yml's build matrix (line 321) and in `TestEveryRunnerLabelIsReviewed`'s approved-label map with a note (internal/cli/runner_labels_test.go:36), matching the claim that the label was reviewed and pinned rather than added bare | .github/workflows/release.yml:321, internal/cli/runner_labels_test.go:36

## FAILURES
(none)

## FIX_HINTS
(none — verdict is PASS)
