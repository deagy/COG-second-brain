VERDICT: PASS
INTEGRATION_CLAIMS_CHECKED: 17

EVIDENCE:
EVIDENCE AC-6 | CP-4 | PASS | cadre HEAD (5c40d6ec) == cli-v0.7.5/plugin-v0.24.5 commit, 0 ahead; cadre-kernel HEAD (d4fb0894) == v0.14.4, 0 ahead; recall HEAD (3bef2354) == v0.3.3, 0 ahead; gloop's "no release, by decision" reason still present in README.md:7 | `git log --oneline <tag>..HEAD` in all three checkouts (empty), `grep` on gloop/README.md — measured independently of CP-5's own claim
EVIDENCE AC-cross | CP-4 | PASS | kernel version agreement across all 5 named sources: `provider/provider.go:40 const KernelVersion = "0.14.4"`, `provider/provider.json` minimum 0.14.4, all 3 tracked `kernel-compatibility.json` (lifecycle, lifecycle-github, lifecycle-gitlab under `plugin/plugins/*/tools/`) say 0.14.4, `plugin/README.md` quotes v0.14.4 twice, `.github/workflows/validate.yml:26 CADRE_KERNEL_REF: v0.14.4` — and `deagy/cadre-kernel` v0.14.4 exists and is Latest. The four stale 0.13.2 kernel-compatibility.json copies found by grep live only under `.claude/worktrees/*`, which `.gitignore:102` excludes — not part of what releases | `grep -rn KernelVersion`, `cat` on each file, `git check-ignore -v`
EVIDENCE AC-cross | CP-4 | PASS | generated shim's checksum matcher (`grep " $ARCHIVE\$" SHA256SUMS`) matches kernel v0.14.4's real, downloaded SHA256SUMS exactly for linux-arm64: expected `db6f0b76...`, matched `db6f0b76...` | `/tmp/claude-1000/cp4-p5/kernel-v0.14.4/SHA256SUMS`, local grep reproduction of `internal/generators/plugin_generation.go:324`
EVIDENCE AC-cross | CP-4 | PASS | kernel v0.14.4 archive (`agentic-sdlc-v0.14.4-linux-arm64.tar.gz`) contains `agentic-sdlc` + `LICENSE`, and its own sha256 matches the SHA256SUMS entry exactly | downloaded archive, `tar tzf`, `sha256sum` vs `grep` in `/tmp/claude-1000/cp4-p5/kernel-v0.14.4`
EVIDENCE AC-cross | CP-4 | PASS | recall's `release.yml:71` checkout step expects cadre's fail-closed contract at `internal/knowledge/testdata/fail-closed-contract.json` — file exists at that exact path in cadre HEAD, and the live "Tag release" run for v0.3.3 (run 33651612476, workflow_dispatch, ~1h before this check) shows both `tag` and `release/release` jobs green, not just a path check | `ls` in ~/sdk/cadre, `gh run view 33651612476 -R deagy/recall`
EVIDENCE AC-cross | CP-4 | PASS | gloop's `ci.yml:35` vendored-fixture guard expects `roster/orchestration/test/select_golden.json` and `conformance-plan.json` in cadre — both exist at cadre HEAD, and gloop's own `main` CI (run 33651727712, pushed same day) is green, confirming P5's cadre changes did not move or remove them | `ls` in ~/sdk/cadre, `gh run list -R deagy/gloop --branch main`
EVIDENCE AC-7 | CP-4 | PASS | independently reproduced the documented install (`docs/INSTALL.md`'s "any runner" row: `curl ... install.sh \| sh`, with `--with-lifecycle`) in a fresh `node:22-bookworm-slim` container, `--platform linux/arm64`, with no cadre/go/agentic-sdlc/~/.cadre present beforehand and only `claude`, `curl`, `git`, `python3` installed as install.sh's own stated prerequisites. Installed cli-v0.7.5/plugin-v0.24.5, kernel v0.14.4. `cadre sdlc --version` → `0.14.4`, exit 0. `cadre knowledge search` → `query is required`, exit 2 (correct usage refusal, not a crash) | `/tmp/claude-1000/cp4-p5/container-output2.log` (own container run, not the worker's `ac7-clean-machine.log`)
EVIDENCE AC-7 | CP-4 | PASS | `cadre doctor` on the same install agrees with `cadre sdlc`: both report `lifecycle kernel: 0.14.4 via packaged plugin /root/.cadre/dist/plugin/plugins/lifecycle/bin/agentic-sdlc` | same container run, doctor output lines 101-114
EVIDENCE P3-survival | CP-4 | PASS | staged-knowledge write/read in the installed binary: `cadre knowledge propose --from-finding` → staged `KS-20260902-...`; `cadre knowledge show-staged` → `"observed_actor": "os:root"`, non-empty | container run, propose+show-staged output
EVIDENCE P2-survival (#249) | CP-4 | PASS | from `/root/subdir/deeper` (no `.git` anywhere in its ancestry, only under the unrelated `~/.cadre/dist` sibling tree), with `~/.agents/knowledge-store/config.json` planted directly at `$HOME` to simulate the global-store file, `cadre knowledge propose` refused to write ("staged knowledge records are per project... Create .agents/knowledge-store/config.json in this project"), i.e. it did NOT alias the file sitting at `$HOME` as a project-local config — the exact aliasing #249 fixed | `/tmp/claude-1000/cp4-p5/container-249.log`
EVIDENCE P4-survival | CP-4 | PASS | `cadre knowledge search --retention-days 30 foo` refuses by name, exit 2, full explanatory text (removed capability, `b418031e`, points at `roster/knowledge-store/SECURITY.md`); same for `--trigger` and `--as-of`. `cadre knowledge delete-staged --reason "--retention-days was never built" ...` is NOT refused for that reason: it proceeds past flag parsing to the store lookup and fails with `no staged record with that id in this store`, exit 1 — a different command failing for a different, expected reason, not a name-refusal (exit 2) | container run, search vs delete-staged output
EVIDENCE AC-6-quality | CP-4 | PASS | published wheels for cli-v0.7.5 = exactly 4 (macosx_11_0_arm64, manylinux_2_17_aarch64, manylinux_2_17_x86_64, win_amd64), matching `PlatformsFor(ProgramCLI)`'s 4 platforms with no extra/missing entries | `gh release view cli-v0.7.5 --json assets`
EVIDENCE archive-hygiene | CP-4 | FAIL (historical, non-blocking) | kernel v0.14.2's archive still carries no `LICENSE` (only `agentic-sdlc`) — pre-dates P5, and nothing in cadre's current HEAD can resolve to it (floor is 0.14.4), but the release itself is still live and downloadable | `/tmp/claude-1000/cp4-p5/kernel-v0.14.2`, `tar tzf`
EVIDENCE harmful-vs-superseded | CP-4 | FAIL (historical, non-blocking) | cli-v0.7.0/plugin-v0.24.0 (6af4047c) pins kernel 0.14.3, whose SHA256SUMS uses the `sha256sum ./*` form — reproduced: the shim's anchored `grep " $ARCHIVE\$"` matches 0 lines against the real v0.14.3 SHA256SUMS. Anyone installing with `--with-lifecycle`/running `cadre sdlc` from this release gets "not listed in the release's SHA256SUMS" — kernel install genuinely fails, not merely superseded | `/tmp/claude-1000/cp4-p5/kernel-v0.14.3/SHA256SUMS`, grep reproduction
EVIDENCE harmful-vs-superseded | CP-4 | FAIL (historical, non-blocking) | cli-v0.7.1/plugin-v0.24.1 (9e9bd6ab) fixes the checksum bug but its `release.yml` build matrix at that commit has no `ubuntu-24.04-arm` leg (only ubuntu-latest/macos-latest/windows-latest) — confirmed by diffing the tagged commit directly. aarch64 Linux installs got "could not obtain the cadre binary for this platform"; other platforms were fine | `git show 9e9bd6ab:.github/workflows/release.yml`
EVIDENCE harmful-vs-superseded | CP-4 | FAIL (historical, non-blocking) | cli-v0.7.2/plugin-v0.24.2 (e7d9a25c) fixes the wheel count and publishes linux/arm64, but `internal/orchestration/kernel_probe.go` at this exact commit has zero references to `PackagedKernelShim` (confirmed by grep on the tagged commit) — the no-Go/packaged install path still cannot resolve a kernel it already downloaded. AC-7's own `cadre sdlc --version` fails on this release under the documented install | `git show e7d9a25c:internal/orchestration/kernel_probe.go \| grep -c PackagedKernelShim` = 0
EVIDENCE harmful-vs-superseded | CP-4 | FAIL (historical, non-blocking) | cli-v0.7.3/plugin-v0.24.3 (ce7b1e01) adds `PackagedKernelShim` but the generated shell launcher's own `sdlc` branch at this exact commit still only calls `config resolve agentic_sdlc.bin_path` and dies with "install Agentic SDLC v0.14.4+" if empty — no fallback to the new function exists yet (confirmed by diffing `internal/generators/plugin_generation.go` at ce7b1e01, vs. the fallback lines present one commit later). CP-3v round1 independently reproduced this exact failure end to end against this same commit | `git show ce7b1e01:internal/generators/plugin_generation.go` lines 2422-2428, cross-checked against CP-3v-round1.md's AC-7 FAIL row
EVIDENCE harmful-vs-superseded | CP-4 | PARTIAL (historical, narrower harm, non-blocking) | cli-v0.7.4/plugin-v0.24.4 (eebb99ba) fixes the launcher fallback — AC-7's two literal commands (`cadre sdlc --version`, `cadre knowledge search`) both work on this release — but `internal/cli/doctor.go:73` at this exact commit still calls `platform.RepoRoot()`, not `FindInstallationRoot()` (confirmed by diff). On the same working install, `cadre doctor` would falsely report no kernel while `cadre sdlc` runs one — a misdiagnosis, not an install failure | `git show eebb99ba:internal/cli/doctor.go` line 73 vs `git show 5c40d6ec` (next commit) which changes it

FAILURES:
- (non-blocking, historical) cadre releases cli-v0.7.0 through cli-v0.7.4 (all now superseded, published within roughly one hour of each other on 2026-09-02) were each genuinely broken for someone who installed that specific tag rather than the latest — not merely missing a later improvement. v0.7.0 could not resolve a kernel at all; v0.7.1 could not obtain an aarch64 Linux CLI binary; v0.7.2 and v0.7.3 both still hit "install Agentic SDLC v0.14.4+" despite a cached kernel; v0.7.4 works end to end but its own `doctor` misdiagnoses it. `docs/INSTALL.md` explicitly documents pinning via `@<tag>` for policy reasons ("If your policy requires a pinned source, append @<tag> ... and own keeping it current"), and GitHub does not mark or hide these superseded releases as broken — they remain listed and downloadable. Only cli-v0.7.5/plugin-v0.24.5 (current) is clean.
- (non-blocking, historical) cadre-kernel v0.14.2's published archive still carries no LICENSE (pre-P5 defect, unrelated to this phase's fix, and unreachable from cadre's current 0.14.4 floor, but still a live download)

FIX_HINTS:
- Consider a retro action (per this phase's own "Not in this phase" note) to gate a release on a smoke-test of the exact install path AC-7 exercises, before tagging — this would have caught v0.7.0 through v0.7.3 before publish rather than after, the same shape of defect the T-05/T-06/T-07/T-08/T-09 chain kept re-finding one step at a time
- Consider marking or deleting cli-v0.7.0 through cli-v0.7.3 as pre-releases/known-broken (GitHub supports a "prerelease" flag and release notes edits after the fact) since `docs/INSTALL.md` invites pinning to any tag by name and none of these four currently carry any warning
- cadre-kernel v0.14.2's archive could be re-cut with LICENSE now that v0.14.3/v0.14.4's packaging step exists, or explicitly marked superseded/deprecated, since it remains a live download with no floor currently pointing at it
