# CP-4 integration verification — production-readiness P6, ROUND 2 (AC-3b)

gloop HEAD `1f37de4` (verified `git log -1`). Read-only against `~/sdk/gloop` in
place (no edits, confirmed `git status --short` clean before and after). All
mutation testing done in a throwaway clone at `/tmp/claude-1000/gloop-mutate`,
deleted at the end of this session. `~/sdk/cadre` read at its own HEAD `5c40d6e`,
untouched.

VERDICT: PASS
INTEGRATION_CLAIMS_CHECKED: 9

EVIDENCE:
EVIDENCE AC-3b-guard-restored-and-falsifies | CP-4 | PASS | `TestDispatchHasNoUnlistedSpecialCase` is back at `internal/docguard/help_text_test.go:1360`. Reintroduced the round-4 bypass in a scratch clone (added `if args[0] == "debug" { return nil }` to `execute()` in `cmd/gloop/cmd/root.go`, before `AddCommand` dispatch): `go build ./...` still compiled clean, and the guard failed for the stated reason — `execute() special-cases "debug" before consulting the command table` — not a compile error. Reverted (`git checkout -- cmd/gloop/cmd/root.go`). | scratch clone `/tmp/claude-1000/gloop-mutate`, this session's `go test ./internal/docguard/... -run TestDispatchHasNoUnlistedSpecialCase -v` output
EVIDENCE AC-3b-guard-set-only-grows-falsifies | CP-4 | PASS | `TestTheGuardSetOnlyGrows` (help_text_test.go:1410) names 20 guards by hand. Deleted `TestDispatchHasNoUnlistedSpecialCase`'s body via the same technique as the original silent removal (a scripted slice between two markers — `func TestDispatchHasNoUnlistedSpecialCase(t *testing.T) {` through the line before `// TestTheGuardSetOnlyGrows.`). `go build ./...` still compiled; the test failed for the stated reason (`TestDispatchHasNoUnlistedSpecialCase is named here and no longer exists`, `19 guards defined ... 20 of them required`) — not by failing to compile. Reverted. At HEAD, unmutated, the same test logs `20 guards defined in internal/docguard, 20 of them required by name` and PASSes, matching `grep -c "^func Test" internal/docguard/*.go` = 20 measured independently. | scratch clone, `go test -run TestTheGuardSetOnlyGrows -v` output (mutated and unmutated)
EVIDENCE AC-3b-three-more-guards-falsify | CP-4 | PASS | Picked three more of the twenty and falsified each independently, not by trusting CP-3-build.md's "every one falsified" claim: (1) `TestEveryGoToolingCommandInTheLiveDocsRuns` — put the trailing-`#`-comment defect the fix commit's own message describes back into README's build line (`go build -o bin/gloop ./cmd/gloop  # builds the CLI`); failed with `malformed import path "#"`, the exact unrunnable-command failure mode. (2) `TestNoLiveDocumentClaimsGloopSelects` — added an undisclaimed "Gloop selects agents for you automatically..." sentence near the top of README; failed citing the exact line and reason. (3) `TestTheMockeryDocMatchesTheMockeryConfig` — removed the `TokenCounter` interface block from `.mockery.yaml`; failed with `MOCKERY_INTEGRATION.md's config example names the interface "TokenCounter" and .mockery.yaml does not`. All three reverted with `git checkout --`; scratch clone confirmed clean after. | scratch clone, three separate `go test -run <name> -v` outputs this session
EVIDENCE integration-hygiene-binary-fix-holds | CP-4 | PASS | `gloop/gloop` is gone from tracking (`git ls-files \| grep -iE "gloop/gloop$"` → empty) and `.gitignore` now excludes it explicitly with an inline explanation of why (`gloop/ is a package directory and -o names an existing one`). Ran the exact old dangerous command from a scratch clone, `go build -o gloop ./cmd/gloop` from the repo root — it wrote a 16MB ELF to `gloop/gloop` as before, but `git status --short` reported nothing: the file is now silently ignored rather than silently tracked. Also ran the corrected documented command (`go build -o bin/gloop ./cmd/gloop`) — clean tree, `bin/` already ignored. Whole-repo scan for tracked binaries (`file` on every `git ls-files` entry, checked for ELF/Mach-O/PE32 magic) found none. | scratch clone, `git ls-files`, `.gitignore`, two build-command trials this session
EVIDENCE guard-count-in-build-record-matches-tree | CP-4 | PASS | CP-3-build.md's closing section states "Twenty guards" and "twenty" for the reconciled count. `TestTheGuardSetOnlyGrows` at HEAD logs `20 guards defined in internal/docguard, 20 of them required by name`; independent `grep -c "^func Test" internal/docguard/*.go` also returns 20. All three numbers agree — the round-1 finding (8/14/17/18 disagreeing) is resolved and the fix commit's own count is not asserted from memory but measured (`t.Logf`, not hardcoded). | `go test -run TestTheGuardSetOnlyGrows -v`; `grep -c "^func Test" internal/docguard/*.go`; evidence/P6/CP-3-build.md closing section
EVIDENCE guard-suite-still-clean-fast-hermetic | CP-4 | PASS | Fresh clone, full suite: `CGO_ENABLED=1 go test -count=1 ./...` — all 37 packages pass, `internal/docguard` 9.0s of an 11.9s wall-clock total. No build tags on either docguard test file (`head -5` on both — plain `package docguard`, no `//go:build`), so nothing there is skip-able by tag; CI's `go test -race -count=1 ./...` (`.github/workflows/ci.yml:58`) runs the untagged suite, confirming the guards are reached in CI, not just locally. | scratch clone timed run this session; `.github/workflows/ci.yml:52-101`
EVIDENCE cadre-fixture-guard-vs-cadre-current-tree | CP-4 | PASS | `~/sdk/cadre` at HEAD `5c40d6e` still has both vendored fixture files at the CI-expected path (`roster/orchestration/test/select_golden.json`, `.../conformance-plan.json`). Ran `pkg/govplan`'s cross-repo guards (`TestTheVendoredFixtureMatchesCadre`, `TestTheCorpusMatchesCadre`, `TestEveryCorpusCaseResolvesAsItsOwnFieldsPredict` — 26 subtests) with `CADRE_SELECT_GOLDEN`/`CADRE_CONFORMANCE_PLAN` pointed at those real files: all PASS. gloop's CI would still pass against cadre's current tree; cadre has not drifted since round 1's identical check. | `CADRE_SELECT_GOLDEN=... CADRE_CONFORMANCE_PLAN=... go test ./pkg/govplan/... -v` this session
EVIDENCE mockery-still-clean | CP-4 | PASS | Re-ran `mockery` (v2.53.6) in the fresh clone after the fix commit: regenerated all 7 mocks named in `.mockery.yaml` (`AuditLogger`, `Plugin`, `Provider`, `ProviderFactory`, `SelectorOption`, `TenantLoader`, `TokenCounter`), `git status --short` and `git diff --stat` both empty afterward. | scratch clone, `mockery` output + `git status`/`git diff --stat` this session
EVIDENCE AC-1-AC-3-AC-6-AC-7-survival-holds | CP-4 | PASS | Round 2's fix commit touched only `.gitignore`, one README line, `gloop/gloop`'s removal, and `help_text_test.go` — none of the P1/P5-related sections. Spot-checked anyway rather than assumed: no licence/badge tokens outside the explanatory removal prose, no `LICENSE` file, install section still reads "Clone and build" / `make install`, release-position paragraph ("main is what you install... not artifacts you can download") intact verbatim. | README.md:5,7,9,31,42 this session

FAILURES:
(none)

Additional observations, not failures:
- CP-3-build.md's "Twenty-two false claims" total (round-over-round narrative
  tally) was not independently re-derived — it's a historical count across nine
  rounds' worth of now-superseded diffs, not a claim about the current tree's
  state, and re-deriving it would mean re-reading every round's report rather
  than observing an artifact. The one number in that section that *is* a claim
  about the current tree — "twenty guards" — was measured and matches.
- No two of the five guards exercised this round or the guards checked in round
  1 assert contradictory things; each failure message named a distinct file/line
  and none fired on another guard's fixture during any of the mutation runs
  above (confirmed by watching full `-v` output, not just the run/skip line for
  the target test).
