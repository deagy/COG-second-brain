VERDICT: FAIL:fixable
LANE: full
CLAIMS_CHECKED: 9
EVIDENCE:
EVIDENCE AC-3b-config-table-complete | CP-3v | PASS | Ran all 16 subcommand entries the README's table names (5 Honoured, 2 Ignored, 9 Rejected) directly against the binary at HEAD `cbd975f`. All match: `config show/setup/update`, `dispatch plan.json --config`, `run plan.json --config` honour the flag (path echoed / file written / config merged after the plan file); `status`, `handoff list` silently ignore it; `handoff get`, `handoff prune` (`unexpected argument "--config"`), `gate list`, `gate create`, `roster show`, `roster validate`, `session list`, `session show`, `init` (`unknown flag "--config"`) all reject it. Table is per-subcommand and accurate for all 16 named rows. | ran binary at `/tmp/claude-1000/gloop-bin`, transcripts in this session
EVIDENCE AC-3b-config-table-omissions | CP-3v | FAIL | 4 subcommands exist that the table does not name: `gate approve`, `gate reject`, `gate skip`, `session delete`. Ran all 4 with `--config`; each is rejected with its own usage line, matching the behaviour of its siblings (`gate list`/`gate create`, `session list`/`session show`) — not a falsehood, but the table is not exhaustive over the CLI's actual subcommand set (16 of 20 named). | ran binary: `gate approve/reject/skip ... --config`, `session delete x --config`
EVIDENCE AC-3b-guard-coverage-count | CP-3v | FAIL | Build record and commit message claim the guard "classifies thirteen subcommands individually." The behavioural loop (`rejects()` table, help_text_test.go:209-225) has 12 entries, not 13 — it omits `config setup` from behavioural testing. `config setup`'s actual `--config` honouring is never exercised by the guard (I verified it manually: `config setup --yes --config ./setupcfg.toml ...` writes to that path). The 13-count only holds for the separate table-naming coverage loop (help_text_test.go:251-258), which checks text presence, not behaviour, and itself excludes `dispatch`, `run`, and `init` (spot-checked elsewhere in the same test function, but not through the coverage loop, and `init` has no dedicated assertion in the test file at all). | `internal/docguard/help_text_test.go:209-225` (12 entries) vs `:251-264` (13 entries); manual run of `config setup --yes --config`
EVIDENCE AC-3b-mutation-flip | CP-3v | FAIL | Mutation-tested "flip a row's classification": moved `handoff prune` from the README's Rejected row to the Ignored row (a false claim — `handoff prune --config` actually rejects with `unexpected argument`). `go test ./internal/docguard/... -run TestTheConfigFlagTableMatchesTheBinary` **passed**. Root cause: the guard's table-coverage loop only does `strings.Contains(table, "`handoff prune`")` — it checks that the name appears somewhere in the table text, never which row/bucket it appears under. The behavioural loop (`rejects()`) is hardcoded against the binary and doesn't read the README at all, so it can't catch a bucket mismatch either. This is the exact class of defect the phase was created to catch, reproduced by moving one entry between rows instead of deleting it. | `/tmp/claude-1000/gloop-mut2/README.md` (mutated then reverted via `git checkout`), `go test` output captured this session
EVIDENCE AC-3b-mutation-drop | CP-3v | PASS | Mutation-tested "drop a subcommand from the table": removed `handoff prune` from the Rejected row entirely (present nowhere in the table). Test failed for the stated reason: `this test classifies \`handoff prune\` and README.md's --config table does not name it` (help_text_test.go:261). | same scratch clone, reverted after
EVIDENCE AC-3b-mutation-heading | CP-3v | PASS | Mutation-tested "rename the table's heading": changed `| Behaviour | Subcommands | How you can tell |` to `| Behavior | ...`. Test failed for the stated reason via `t.Fatal`: `README.md has no --config behaviour table; this check now verifies nothing` (help_text_test.go:244) — a clean, named failure, not a compile error or a silent pass. | same scratch clone, reverted after; `go build ./...` and full `go test ./internal/docguard/...` both green post-revert
EVIDENCE AC-3b-matcher-not-overbroad | CP-3v | PASS | `handoff prune` is the only call site of `"unexpected argument"` in non-test source (`cmd/gloop/cmd/handoff.go:317`), so no other command can trip that branch of the matcher today. Confirmed the matcher's "usage:" hits are specifically caused by `--config`, not coincidental: `handoff get bogus-id` (no `--config`) fails with an ordinary not-found error (`handoff: handoff snapshot not found: bogus-id`, no "usage:"); the same command with `--config` fails with `usage: gloop handoff get <id> [--file <path>]`. Also confirmed `roster show` (no `--config`, no roster) fails with `no roster found: ...` (no "usage:"/"unknown flag"/"unexpected argument"), so the matcher would not misclassify that ordinary failure if it were ever probed without `--config`. No false-positive found. | `grep -rn '"unexpected argument"' --include=*.go`, binary runs above
EVIDENCE AC-3b-live-index-still-holds | CP-3v | PASS | Re-ran full `internal/docguard` suite (10 tests) at HEAD `cbd975f`: all pass, `TestEveryRecordSaysItIsOne` still checks 22 records, `TestTheLiveListInTheIndexMatchesThisGuard` and `TestConfigShowNeverContradictsItself` unaffected by this phase's change. No regression introduced elsewhere. | `go test ./internal/docguard/... -v`, this session
FAILURES:
- AC-3b | README.md `--config` table (bucket assignment) | `TestTheConfigFlagTableMatchesTheBinary`'s coverage check verifies a subcommand *name* appears somewhere in the table's text but never verifies which *row* (bucket) it appears under. Moving `handoff prune` from Rejected to Ignored — a false claim, contradicted by the binary — leaves the test green. This is the same failure mode the whole phase exists to prevent, now hiding one level deeper: the guard was widened to classify per-subcommand but not to tie each classification to its claimed bucket in the doc.
- AC-3b | README.md `--config` table completeness | `gate approve`, `gate reject`, `gate skip`, `session delete` exist and take `--config` (all rejected, consistent with siblings) but are named nowhere in the table or the guard. Not a false claim today, but unguarded — a future behavioural change to any of these four would go undetected by the doc or the test.
- AC-3b | CP-3-build.md / commit message | The claim "classifies thirteen subcommands individually" overstates the behavioural guard, which classifies 12 (`config setup` is coverage-checked for table-presence only, never run against the binary by the guard; `dispatch`, `run`, `init` are covered by separate ad hoc assertions or, for `init`, not covered by the guard at all).
FIX_HINTS:
- AC-3b | Parse each table row's own bucket label and assert the guard's independently-derived classification (honoured/ignored/rejected) matches the row the subcommand actually sits under in the README, not just that the name appears in the table text somewhere.
- AC-3b | Either add `gate approve`, `gate reject`, `gate skip`, `session delete` to the table's Rejected row and the guard's two loops, or state explicitly that the table covers a representative subset per group rather than every subcommand.
- AC-3b | Add `config setup` and `init` to the behavioural `rejects()` loop (currently only table-presence-checked or untested), and fold the standalone `dispatch`/`run` assertions into the same coverage list so "thirteen" (or whatever the true count becomes) is a number the loop itself produces rather than one asserted in prose.
