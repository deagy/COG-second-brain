VERDICT: FAIL:fixable
LANE: full
CLAIMS_CHECKED: 19
EVIDENCE:
EVIDENCE AC-3b-scope | CP-3v | PASS | 3 spot-checked records (PHASE3-E5-OTEL, HYGIENE-FIX-PLAN, CODE-REVIEW.md) all carry the "Historical record, not a description of the shipped system" banner as the first line under the title, before any body claim | `docs/PHASE3-E5-OTEL-OBSERVABILITY.md:3`, `docs/HYGIENE-FIX-PLAN.md:3`, `docs/CODE-REVIEW.md:3`
EVIDENCE AC-3b-config-honoured | CP-3v | PASS | `config show/setup/update`, `dispatch`, `run` all read `--config`: `config show --config x` echoes the path+provider+model; `config setup --config x` writes to that path; `dispatch/run --config x plan.json` proceeds to provider creation using that file | ran binary at HEAD `dce0b85` in `/tmp/claude-1000/rostertest`
EVIDENCE AC-3b-config-ignored-status | CP-3v | PASS | `gloop status --config <valid path>` returns `"configPath": null, "message": "no gloop config found"` for a file that exists and loads — matches README verbatim | ran binary, see transcript
EVIDENCE AC-3b-config-ignored-handoff | CP-3v | FAIL | README lists bare `handoff` in the "Ignored, silently" bucket. `handoff list --config <path>` is silently ignored (matches claim), but `handoff get <id> --config <path>` and `handoff prune --config <path>` are **rejected**: `Error: usage: gloop handoff get <id> [--file <path>]` / `Error: unexpected argument "--config" (usage: gloop handoff prune [flags])`. The guard `TestTheConfigFlagTableMatchesTheBinary` only probes `handoff list`, never `get`/`prune`, so it cannot catch this. | ran binary: `handoff get x --config ./mycfg.toml`, `handoff prune --config ./mycfg.toml`; guard source `internal/docguard/help_text_test.go:203`
EVIDENCE AC-3b-config-rejected | CP-3v | PASS | `gate list`, `roster show`, `session list` each print that subcommand's own usage line (`Error: usage: gloop gate list [--task <task-id>]`, etc.); `init --config` is rejected too (different message shape, `unknown flag "--config"`, but still refused, consistent with "Rejected") | ran binary, see transcript
EVIDENCE AC-3b-positional-trap | CP-3v | PASS | `gloop dispatch --config x.toml plan.json` fails with `failed to read plan file: open --config: no such file or directory`, quoted verbatim in README.md:194; `gloop dispatch plan.json --config x.toml` works | ran binary
EVIDENCE AC-3b-help-doubling | CP-3v | PASS | `gloop --help` shows `Usage: gloop` (not `gloop gloop`), trailer reads `Use "gloop [command] --help"`. Mutation-reverted `invocation()` in scratch clone to reproduce `"gloop " + c.Use` unconditionally → `TestNoHelpTextDoublesTheProgramName` failed for exactly that string | `/tmp/claude-1000/gloop-mut/cmd/gloop/cmd/help.go`
EVIDENCE AC-3b-orphan-example | CP-3v | PASS | root Examples opens with `# Dispatch a plan for execution` immediately followed by a command; no orphan comment. Reintroduced `# Select agents for a task` + blank line in scratch clone → `TestAnExampleAlwaysCarriesItsCommand` failed for that exact comment | `/tmp/claude-1000/gloop-mut/cmd/gloop/cmd/root.go`
EVIDENCE AC-3b-removed-command-error | CP-3v | PASS | `gloop roster validate` (no roster) error text contains no `gloop select` or `roster plan`; `strings` on the binary finds neither string anywhere. Reintroduced `roster plan <roster> --task <task>` wording in scratch clone → `TestNoErrorMessageOffersARemovedCommand` failed on that string | `/tmp/claude-1000/gloop-mut/cmd/gloop/cmd/roster.go`
EVIDENCE AC-3b-config-show-states | CP-3v | PASS | Ran all three states directly: valid → `configExists:true` + provider/model; malformed-but-present → `configExists:true` + "found but could not be loaded: ..."; absent → `configExists:false` + "no gloop config at that path: ...". No boolean/message contradiction in any payload | ran binary against `mycfg.toml`, `bad.toml`, `nope.toml`
EVIDENCE AC-3b-docguard-configshow-mutation | CP-3v | PASS | Reintroduced the original defect (`configExists` hardcoded `false` in the malformed-but-present branch) → `TestConfigShowNeverContradictsItself` failed citing exactly that contradiction | `/tmp/claude-1000/gloop-mut/cmd/gloop/cmd/config.go`
EVIDENCE AC-3b-docguard-selfcheck | CP-3v | PASS | Broke the guard's own "valid" fixture (`provider = "anthropic"` as a string instead of a table) → test failed with its explicit self-check message ("the valid fixture did not load ... this test would then be asserting nothing"), not a silent pass | `/tmp/claude-1000/gloop-mut/internal/docguard/help_text_test.go` (reverted after)
EVIDENCE AC-3b-docguard-livelist-mutation | CP-3v | PASS | Removed `MOCKERY_INTEGRATION.md` from docs/README.md's live-list section (leaving `liveDocs` untouched) → `TestTheLiveListInTheIndexMatchesThisGuard` failed noting the exact name that diverged | `/tmp/claude-1000/gloop-mut/docs/README.md` (reverted after)
EVIDENCE AC-3b-docguard-banner-mutation | CP-3v | PASS | Stripped the historical-record banner from `docs/PHASE1-REVIEW.md` → `TestEveryRecordSaysItIsOne` failed naming that file | `/tmp/claude-1000/gloop-mut/docs/PHASE1-REVIEW.md` (reverted after)
EVIDENCE AC-3b-docguard-provider-mutation | CP-3v | PASS | Removed "Cohere" from README's Multi-Provider bullet → `TestTheProviderListNamesEveryProvider` failed naming the missing provider | `/tmp/claude-1000/gloop-mut/README.md` (reverted after)
EVIDENCE AC-3b-docguard-deprecation-mutation | CP-3v | PASS | Both deprecation-parity directions verified: (a) added a false "MatchRoutes is deprecated" sentence to README → `TestProseDoesNotDeprecateWhatTheSourceKeeps` failed; (b) marked an unmentioned symbol (`DefaultCircuitBreakerConfig`) `// Deprecated:` in source → `TestEveryDeprecatedSymbolIsAnnounced` failed. (Note: an initial probe using `MatchRoutes` for direction (b) passed only because that name is already mentioned incidentally in `CHANGELOG.md`/`docs/ROSTER.md` for unrelated reasons — the guard checks mention-only, not migration-path quality, by its own documented design; not a defect.) | `/tmp/claude-1000/gloop-mut/README.md`, `/tmp/claude-1000/gloop-mut/pkg/middleware/circuitbreaker.go` (both reverted after)
EVIDENCE AC-3b-config-toml-example | CP-3v | PASS | Extracted the full `config.toml` fenced block from README.md verbatim into a scratch file and loaded it with `gloop config show --config`: reports `provider: anthropic`, `model: claude-sonnet-4-20250514`, `configExists: true` — no load error despite `[knowledge]`, `[circuit_breaker]`, `[rate_limit]`, `[providers.openai]` sections all present | `/tmp/claude-1000/rostertest/full-config.toml`
EVIDENCE AC-3b-webui-metrics-plugin | CP-3v | PASS | All 3 documented web-UI endpoints (`/api/sessions`, `/api/metrics`, `/health`), all 8 documented metric name constants, the `Plugin` interface's 7 methods, and all 13 `pkg/*` paths in the Package Structure table exist verbatim in source | `pkg/webui/webui.go:129-131`, `pkg/logging/metrics.go:212-219`, `pkg/plugin/plugin.go:10-20`, dir listing
FAILURES:
- AC-3b | `--config` contract table (README.md:185-187) | README's "Ignored, silently" row names bare `handoff` alongside `status`, backed only by a `gloop status --config <path>` example (not a handoff example). Actual behavior: `handoff list --config <path>` is silently ignored, but `handoff get <id> --config <path>` and `handoff prune --config <path>` are rejected with usage/unknown-argument errors. The claim as stated ("handoff" unqualified, in the same bucket as status) is false for 2 of 3 of handoff's subcommands. `TestTheConfigFlagTableMatchesTheBinary` only exercises `handoff list`, so this gap is invisible to the guard suite.
FIX_HINTS:
- AC-3b | Either (a) split the "Ignored, silently" row's `handoff` entry to name only `handoff list`, and add `handoff get`/`handoff prune` to the "Rejected" row (or a documented fourth case if their rejection shape differs enough to matter), and extend `TestTheConfigFlagTableMatchesTheBinary`'s `rejects()` table to cover `{"handoff","get"}`/`{"handoff","prune"}` so the guard can catch a future regression; or (b) if the intent is that all of `handoff` behaves uniformly, that's a code inconsistency between `list` and `get`/`prune`'s flag handling — fix the code instead of the doc, then confirm with the same commands.
