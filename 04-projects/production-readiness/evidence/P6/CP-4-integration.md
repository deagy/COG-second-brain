# CP-4 integration verification — production-readiness P6 (AC-3b)

gloop HEAD `a3caaa3` (verified `git log -1`). Read-only: all checks run against
`~/sdk/gloop` in place (nothing edited — one incidental local mutation to a tracked
binary from running `go test`/`go build` in-tree was found and reverted with
`git checkout -- gloop/gloop`, confirmed by `git status --short` returning clean) plus
a scratch clone at `/tmp/claude-1000/gloop-mockery-check`. `~/sdk/cadre` read at its own
HEAD `5c40d6e`, untouched.

VERDICT: FAIL:fixable
INTEGRATION_CLAIMS_CHECKED: 11

EVIDENCE:
EVIDENCE AC-3b-guard-regression | CP-4 | FAIL | `TestDispatchHasNoUnlistedSpecialCase` (added commit `7f7406f`, round 4, after a verifier demonstrated a subcommand can be dispatched by special-casing `args[0]` in `execute()` before `AddCommand` registration — runs, silently ignores `--config`, appears in no `--help` output, trips no other guard) was silently deleted in commit `7a5a3dd` (round 6/7's commit, titled "four categories nobody had checked, and a tagged build that never compiled" — its message never mentions removing this guard). Confirmed absent: `grep -rn "DispatchHasNoUnlistedSpecialCase\|SpecialCase" --include="*.go" .` in the current tree returns nothing, and `git log --all -S "DispatchHasNoUnlistedSpecialCase" --oneline` shows only the add (`7f7406f`) and the removing diff inside `7a5a3dd`. `cmd/gloop/cmd/root.go`'s `execute()` at HEAD still has exactly the special-case shape the guard was written to bound (`args[0] == "help"`), so today's tree happens to be fine — but the regression-detection for that specific, previously-demonstrated defect class is now zero. CP-3-build.md's closing "AC-3b, decided" section still narrates round 4's guard as standing and claims "Fourteen guards, every one falsified by reintroducing the defect it catches"; that claim is false of the guard set actually at HEAD. | `internal/docguard/help_text_test.go` (absent), `git show 7f7406f` (add), `git show 7a5a3dd -- internal/docguard/help_text_test.go` (silent removal, lines ~180–225 of the diff), `cmd/gloop/cmd/root.go:64-77`, evidence/P6/CP-3v-round4.md `EVIDENCE AC-3b-smuggled-subcommand-escape`
EVIDENCE AC-3b-guard-count-unreconciled | CP-4 | FAIL | Guard-count bookkeeping never converged. checkpoints.tsv/CP-3-build.md's CP-3 row says "8 guards, all falsified"; CP-3-build.md's closing line says "Fourteen guards"; round 9's own CP-3v report (verified at `bd345bc`, one commit before HEAD) says "internal/docguard's full 17-guard suite" / "17 top-level guards all pass"; the actual tree at HEAD `a3caaa3` (unchanged guard count from `bd345bc` — `a3caaa3` only edits one existing test body) defines 18 top-level `Test` functions (`grep -c "^func Test" internal/docguard/*.go` → 16 + 2), of which 17 execute and one (`TestEveryDeprecatedSymbolIsAnnounced`) legitimately self-skips ("nothing is marked deprecated" — a real conditional, not a broken guard). None of the four numbers (8, 14, 17, 18) agree, and none of them accounts for the one guard that was actually deleted (finding above). The "Fourteen guards" the CP-4 brief itself was told to check as a system is not the guard set that exists. | `go test ./internal/docguard/... -v` this session (18 run/skip lines, 17 PASS + 1 SKIP); `grep -c "^func Test" internal/docguard/*.go`; evidence/P6/CP-3-build.md (closing section); evidence/P6/CP-3v-round9.md ("17-guard suite")
EVIDENCE AC-3b-tracked-binary | CP-4 | FAIL | A 16MB compiled ELF binary, `gloop/gloop` (ARM64, statically linked, not stripped), is tracked in git inside the `gloop/` package source directory and present at HEAD. `git log --diff-filter=A --oneline -- gloop/gloop` shows it was first added in commit `b8ba719` — round 6's commit, "fix(docs): run the commands the documents give, and sync the mockery doc" — whose message is entirely about documentation fixes and says nothing about adding a binary. `.gitignore` excludes only `bin/` (the Makefile's actual build target); nothing excludes `gloop/gloop`, and no docguard test checks for stray tracked binaries. Running this session's `go test`/`go build` commands in `~/sdk/gloop` caused the tracked copy to be rebuilt locally (`git diff --stat` showed `gloop/gloop \| Bin 16863411 -> 16777621 bytes` after routine test runs) — restored via `git checkout -- gloop/gloop` before finishing. That the toolchain silently rewrites a file `git status` then reports as modified, on an ordinary `go test ./...`, is itself evidence this was accidental, not a deliberate vendored artifact. | `git log --diff-filter=A --oneline -- gloop/gloop` → `b8ba719`; `git show --stat b8ba719` (commit message, no binary mention); `.gitignore`; `file gloop/gloop`
EVIDENCE AC-1/AC-3-survival | CP-4 | PASS | P1's licensing/badge/removed-command settlement survives P6's README/docs rewrite. `README.md` carries no Go-Report-Card/pkg.go.dev/licence badges and no licence claim (`grep -niE "badge\|license\|licence\|pkg.go.dev\|goreportcard" README.md` → only the prose explaining they were removed and the "no licence" License section); no `LICENSE` file exists (`ls LICENSE*` → none). `CHANGELOG.md`'s `[Unreleased] / Removed` entry and `docs/ROSTER.md`'s opening blockquote both describe `gloop select`, `roster.Select`, `gloop roster plan`, `pkg/selector` in explicit removal-note framing ("Removed.", "gone", "were announced as deprecated"), not as current behaviour — the only place elsewhere those tokens appear is in files classified as records (`docs/CODE-REVIEW.md`, `docs/PHASE1-REVIEW.md`, `docs/PHASE3-REVIEW.md`, `docs/ENHANCEMENT_PLAN.md`, `docs/REQUIREMENTS-MAP.md`), all under the historical-record banner. | README.md:1-568; CHANGELOG.md:8-20; docs/ROSTER.md:1-10
EVIDENCE AC-6/AC-7-survival | CP-4 | PASS | P5's install-instructions and release-position statements survive. README's Installation section reads "Clone and build" and its fenced example (`git clone git@github.com:deagy/gloop.git`, `cd gloop`, `make install`) matches the actual `Makefile` (`install:` target runs `$(GO) install $(PKG)`, comment "Install the gloop binary into $GOBIN (or $GOPATH/bin)"). The release-position paragraph ("`main` is what you install.… `v0.1.0` and `v0.2.0` are markers in the history, not artifacts you can download") is present verbatim, matching spec.md's AC-6 traceability row ("gloop none, by a decision written into its own README"). | README.md:7,26-40; Makefile:27-33
EVIDENCE guard-suite-runs-and-compiles | CP-4 | PASS | Fresh clone (`/tmp/claude-1000/gloop-mockery-check`, HEAD `a3caaa3`): `go build ./...`, `go vet ./...`, `go vet -tags integration ./...` all clean; `CGO_ENABLED=1 go test -count=1 ./...` green across all 37 packages including `internal/docguard` (9.9s) and the previously-broken `-tags integration ./pkg/roster/...` (18 subtests, all PASS, 1.4s) — the "test file that had not compiled for weeks" the brief cites is fixed and confirmed compiling/passing directly, not by re-reading the build record. | command output this session, `/tmp/claude-1000/gloop-mockery-check`
EVIDENCE guard-suite-timing | CP-4 | PASS | Full `go test -count=1 ./...` in the fresh clone: 12.7s wall / 36.8s CPU across 37 packages; `internal/docguard` alone 9.9s of that. Not slow enough to be a plausible skip target relative to the whole suite. | timed command output this session
EVIDENCE guard-suite-no-network-or-machine-dependence | CP-4 | PASS | All 18 docguard `exec.Command` call sites shell out to `go build`/`go vet`/`go list` or the binary the guard itself just built (`go build -o t.TempDir()/gloop ../../cmd/gloop`) — no network client, no hardcoded absolute path, no `replace` directive in `go.mod` (`grep -n "replace" go.mod` → none). `pkg/govplan`'s two cross-repo fixture guards (`TestTheVendoredFixtureMatchesCadre`, `TestTheCorpusMatchesCadre`) fall back to a sibling `../cadre` checkout or `CADRE_*` env vars and explicitly `t.Fatal` (not skip) when `CI` is set and nothing is reachable — the exact failure mode the brief's "twice recorded a check that passed locally off something no runner has" warns against, already guarded against by design. | internal/docguard/help_text_test.go (exec.Command sites); pkg/govplan/govplan_test.go:79-104; go.mod
EVIDENCE cadre-fixture-guard-vs-cadre-current-tree | CP-4 | PASS | `.github/workflows/ci.yml` checks out `deagy/cadre` to `.cadre-origin` and points `CADRE_SELECT_GOLDEN`/`CADRE_CONFORMANCE_PLAN` at `roster/orchestration/test/{select_golden,conformance-plan}.json`. Both files exist at that exact path in `~/sdk/cadre` (HEAD `5c40d6e`). Ran gloop's guards with those env vars pointed at cadre's real current tree: `TestTheVendoredFixtureMatchesCadre`, `TestTheCorpusMatchesCadre`, and `TestEveryCorpusCaseResolvesAsItsOwnFieldsPredict` (26 subtests) all PASS — gloop's CI would pass against cadre's current state, not just against a stale local copy. The `roster-contract` job's live GitHub clone of `cadre-roster` is correctly `continue-on-error: true`, so it cannot block CI on a network failure. | `~/sdk/cadre/roster/orchestration/test/{select_golden,conformance-plan}.json`; `CADRE_SELECT_GOLDEN=... CADRE_CONFORMANCE_PLAN=... go test ./pkg/govplan/... -v` this session; .github/workflows/ci.yml:12-38
EVIDENCE mockery-clean-tree | CP-4 | PASS | `.mockery.yaml` names 7 interfaces (`Plugin`, `TenantLoader`, `AuditLogger`, `Provider`, `ProviderFactory`, `SelectorOption`, `TokenCounter`); `test/mocks/` holds exactly 7 `mock_*.go` files matching those names. Ran `mockery` (v2.53.6) in the fresh clone: regenerated all 7, zero "no such interface" warnings, `git diff --stat` and `git status --short` both empty afterward — the regenerated tree is byte-identical to what's committed. | `/tmp/claude-1000/gloop-mockery-check`, `mockery` output and `git diff` this session
EVIDENCE docs-readme-four-live-set-and-banners | CP-4 | PASS | `docs/README.md` names exactly four live documents (`ARCHITECTURE.md`, `ROSTER.md`, `ROSTER_PEER_EXCHANGE.md`, `MOCKERY_INTEGRATION.md`) plus root `README.md` called out separately as "the front door." `TestTheLiveListInTheIndexMatchesThisGuard` PASSes against this set (byte-derived, not read-and-trust). `TestEveryRecordSaysItIsOne` logs "checked 22 record(s)" and PASSes — every non-live document under `docs/` carries the historical banner. | docs/README.md:1-20; `go test ./internal/docguard/... -run 'TestTheLiveListInTheIndexMatchesThisGuard\|TestEveryRecordSaysItIsOne' -v` this session
EVIDENCE root-help-text-residue-fix-holds | CP-4 | PASS | Round 9's residue note (`gloop --help`'s Long string claimed "selecting agents," contradicting the established cadre-selects fact, and no live document quoted it so no guard caught it) was fixed in the final commit `a3caaa3`. Built the binary and ran `gloop --help` and `gloop roster --help` live: root now reads "Selecting agents is cadre's -- gloop reads the plan cadre produces with pkg/govplan," roster reads "Route selection and dispatch-plan generation are cadre's, not gloop's." No "gloop gloop" doubling, no reference to removed commands. | `/tmp/claude-1000/gloop-bin --help`, `/tmp/claude-1000/gloop-bin roster --help` this session

FAILURES:
- AC-3b | TestDispatchHasNoUnlistedSpecialCase silently deleted in commit `7a5a3dd`, two rounds after it was added to close a verifier-demonstrated dispatch-bypass defect (round 4). No guard at HEAD detects a subcommand dispatched via a second `args[0]`-literal branch in `execute()` outside `AddCommand` registration. CP-3-build.md's closing summary does not disclose the removal and still implies the guard stands.
- AC-3b | CP-3-build.md's final guard-count claim ("Fourteen guards") does not match the guard set at HEAD (18 defined, 17 active) or round 9's own count (17), and none of the recorded counts subtracts the one guard that was actually deleted. The tally was never reconciled against the tree it describes.
- integration-hygiene | `gloop/gloop`, a 16MB compiled ELF binary, was accidentally committed in round 6's commit `b8ba719` and remains tracked at HEAD inside the `gloop/` package source directory. Not excluded by `.gitignore` (which covers only `bin/`), not caught by any guard, and silently rewritten by an ordinary `go test`/`go build` in the checkout (confirmed and reverted this session).

FIX_HINTS:
- Restore `TestDispatchHasNoUnlistedSpecialCase` (or an equivalent check with the same falsification property, verified again by reintroducing the round-4 bypass) and correct CP-3-build.md's closing tally to state the true guard count at HEAD, reconciled by running `grep -c "^func Test" internal/docguard/*.go` rather than carried forward from an earlier round's count.
- `git rm gloop/gloop`, add `gloop/gloop` (or a broader compiled-binary pattern) to `.gitignore`, and commit the removal separately from any doc/guard work so it's traceable.
