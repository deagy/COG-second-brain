VERDICT: FAIL:fixable
LANE: full
CLAIMS_CHECKED: 14
EVIDENCE:
EVIDENCE AC-3b-smuggled-subcommand-escape | CP-3v | FAIL | Added a fully dispatchable subcommand `gloop smuggled` by special-casing it directly inside `execute()` in `cmd/gloop/cmd/root.go` (before the `rootCmd.cmds` loop), never calling `AddCommand`. Built binary: `gloop smuggled` and `gloop smuggled --config /nonexistent` both run and print identically (the flag is silently accepted and ignored — a real, undocumented "ignored, silently" case). `gloop --help \| grep -i smuggled` finds nothing. Ran `go test ./internal/docguard/... -run 'TestTheConfigFlagTableMatchesTheBinary\|TestTheHelpEnumerationIsComplete' -v`: **both PASS**. Neither guard notices — `enumerateSubcommands` only sees `--help` output (which never lists it), and `TestTheHelpEnumerationIsComplete`'s registered-count check only scans `AddCommand(...)` call sites via regex, which this bypass never touches, so `registered` and `enumerated` stay in lockstep at the old totals. This is exactly the shape the brief asked to attack ("can a subcommand escape both the table and the exclusion set") and it does, cleanly. | mutated/reverted `/tmp/claude-1000/gloop-verify4/cmd/gloop/cmd/root.go` (git stash), test output and binary runs this session, binary at `/tmp/claude-1000/gloop-verify4-bin`
EVIDENCE AC-3b-config-toml-example-provider-comment | CP-3b | FAIL | README.md's `config.toml` example (the block `TestConfigShowNeverContradictsItself`/CP-3-build.md claims "still loads" and "held") carries `name = "anthropic"                # anthropic \| google \| mistral \| http` — naming only 4 of the 6 valid provider types. `pkg/types/provider.go`'s `IsKnownProviderType` (the stated "single source of truth") accepts `anthropic, openai, google, mistral, cohere, http`; `pkg/runtime/registry.go` imports all six provider packages including `openai` and `cohere`; README's own "**Multi-Provider**" bullet (checked by `TestTheProviderListNamesEveryProvider`) correctly lists all six. `TestTheProviderListNamesEveryProvider` only scans the line containing `**Multi-Provider**`; it never reads the toml fenced block, so this second, live occurrence of the same enumeration goes unchecked. A reader following the example's own inline comment would conclude `openai`/`cohere` are not valid `[provider] name` values, which is false. | `/tmp/claude-1000/gloop-verify4/README.md` (toml fence, unmutated), `pkg/types/provider.go:157-166`, `pkg/runtime/registry.go:7-12`, `internal/docguard/help_text_test.go:692-724`
EVIDENCE AC-3b-round1-handoff-uniform-bucket | CP-3v | PASS | Mutated README to state `handoff` uniformly ignored (moved `handoff get`, `handoff prune` into the Ignored row). `TestTheConfigFlagTableMatchesTheBinary` fails, printing the correct three-row block to paste in. Generation, not parsing, makes this shape structurally uncatchable-as-a-pass now. | mutated/reverted README.md this session
EVIDENCE AC-3b-round2-bucket-flip | CP-3v | PASS | Mutated README moving `session delete` from Rejected to Ignored (a false claim — `session delete` treats `--config`/path as stray positionals and errors with a `usage:` line, confirmed by reading `parseSessionFlags`/`runSessionDelete` in `cmd/gloop/cmd/session.go`). Test fails with the correct block. | mutated/reverted README.md this session
EVIDENCE AC-3b-round3-dispatch-run-exclusion | CP-3v | PASS | Deleted the README paragraph documenting `dispatch`/`run`'s positional-trap rule (both `--config x plan.json` and `plan.json --config x` mentions). Test fails twice: `"dispatch" is excluded from the generated table and README.md does not document it separately either, so it is covered by nothing` / same for `"run"`. | mutated/reverted README.md this session
EVIDENCE AC-3b-positional-trap-message | CP-3v | PASS | `gloop dispatch --config x.toml plan.json` and `gloop run --config x.toml plan.json` both emit `Error: failed to read plan file: open --config: no such file or directory`, matching README's quoted text verbatim. | binary run this session, `/tmp/claude-1000/gloop-verify4-bin`
EVIDENCE AC-3b-help-text-fixes | CP-3v | PASS | `gloop --help` opens `Usage: gloop` (no doubled name); `--help \| grep -i "gloop gloop\|select agent"` and `roster validate \| grep -i "roster plan\|select"` both found nothing (exit 1). | binary run this session
EVIDENCE AC-3b-generated-table-content | CP-3v | PASS | Full `go test ./internal/docguard/...` on the unmodified checkout: 11 tests, all PASS/SKIP (one legitimately skipped: nothing currently deprecated). Confirms baseline is green before any mutation. | `/tmp/claude-1000/gloop-verify4`, test output this session
EVIDENCE AC-3b-classifier-noise-check | CP-3v | PASS | Read every classified command's source (`status.go`, `config.go`, `handoff.go`, `session.go`, `gate.go`, `init.go`) for output containing timestamps/random ids/paths that could differ between the two probe configs for reasons unrelated to `--config`. `status`/`config show` include `configLastModified` (mtime) and `configPath`, but `status` calls `resolveConfigPath(nil)` — ignoring `args` entirely — so both probe runs produce byte-identical JSON regardless of flag content, correctly landing "ignored". No classified command embeds a random id or free-running clock in its probed output path. | source reads this session, no code changes
EVIDENCE AC-3b-config-show-three-states | CP-3v | PASS | `TestConfigShowNeverContradictsItself` passes unmutated; re-read `runConfigShow` in `config.go` confirms `configExists`/`message` are set consistently for valid/malformed/absent, no contradiction in source. | `internal/docguard/help_text_test.go:617-683`, `cmd/gloop/cmd/config.go:140-186`
EVIDENCE AC-3b-config-flag-table-honoured-mechanics | CP-3v | PASS | `configProbeArguments`/`classifyConfigFlag` design re-read: two configs differ only in `model`, fresh files per subcommand (fixes round-3-era shared-fixture bug), output not normalized (so a write-target echo like `Wrote config to <path>` counts as honoured), rejection matched on `usage:`/`unknown flag`/`unexpected argument`. Matches build record's description; no discrepancy found. | `internal/docguard/help_text_test.go:206-360`
EVIDENCE AC-3b-help-enumeration-groups-fragility | CP-3v | FAIL | `TestTheHelpEnumerationIsComplete`'s group set is a hardcoded five-element list (`config, gate, handoff, roster, session`) in `enumerateSubcommands`. A new group added the normal way (a command with its own children, registered via `AddCommand`) would not be recognized as a group by the enumerator (treated as one flat subcommand) while its children *would* be counted individually by the `AddCommand`-argument regex in the completeness test — this does not pass silently (both `TestTheConfigFlagTableMatchesTheBinary`, via an unknown-subcommand `t.Errorf`, and `TestTheHelpEnumerationIsComplete`, via a count mismatch, would fail loudly), so it is a maintenance trap rather than a silent hole, but it is a second hardcoded list doing the same job `unclassifiable`/`liveDocs` already do elsewhere in this file without a cross-check, and it will produce a confusing failure rather than a clear one the day a sixth group ships. | `internal/docguard/help_text_test.go:269-270`, reasoned, not executed (lower priority than the two FAILs above; noted for the fix)
EVIDENCE AC-3b-records-and-live-list | CP-3v | PASS | `TestEveryRecordSaysItIsOne` logs "checked 22 record(s)"; `TestTheLiveListInTheIndexMatchesThisGuard` passes against the current `liveDocs` set and `docs/README.md`. Not independently re-audited beyond re-running the guards (round 1-3 already sampled this directly and it was not in question for round 4). | test output this session
FAILURES:
- AC-3b | Enumeration anchor / dispatch bypass | A subcommand can be made fully dispatchable (reads args, honours or ignores `--config`, produces real output) by special-casing it inside `execute()` in `cmd/gloop/cmd/root.go` without ever calling `AddCommand`. It is invisible to `gloop --help`, invisible to `enumerateSubcommands`, absent from the generated `--config` table, and does not trip `TestTheHelpEnumerationIsComplete` because that guard's registered-count anchor is a text regex over `AddCommand(...)` call sites rather than the actual dispatch table `execute()` consults (`rootCmd.cmds`, itself populated only by `AddCommand` — but `execute()` also contains a hardcoded `args[0] == "help"` branch already, proving special-cased dispatch outside `AddCommand` is an established pattern in this file, not a contrivance). This is the fourth round's version of the same defect, one level higher: the generator/table pair is now sound for anything registered through `AddCommand`, but "registered through `AddCommand`" and "dispatchable" are not actually the same set, and nothing asserts they are.
- AC-3b | config.toml example, provider-name comment | The inline comment `# anthropic | google | mistral | http` on the `config.toml` example's `name = "anthropic"` line names 4 of the 6 valid provider types, omitting `openai` and `cohere` — both accepted by `IsKnownProviderType` and both built by `registry.go`, and both named in the adjacent, guarded "Multi-Provider" bullet three lines above the block. `TestTheProviderListNamesEveryProvider` checks only the bullet, not this second enumeration inside the toml fence CP-3-build.md separately claims "held" in full.
FIX_HINTS:
- AC-3b | Anchor `TestTheHelpEnumerationIsComplete` (or a new guard) to the actual dispatch path rather than to `AddCommand` call-site text: e.g. have `execute()` route everything through a single table (`rootCmd.cmds`, recursively) with no special-cased top-level string branches, or, if a special-cased branch must exist (as `help` already does), maintain and assert against an explicit registry of such branches so a new one cannot be added silently. A regex over source text can always fail to see a new way of writing dispatch; the same lesson that produced the generated table applies one layer up.
- AC-3b | Extend `TestTheProviderListNamesEveryProvider` (or add a sibling check) to also scan the `config.toml` fenced example's provider-name comment against the same registry-derived provider list, or replace the hardcoded `anthropic | google | mistral | http` comment with one generated the same way the Multi-Provider bullet is checked.
- AC-3b | Note only, not blocking: consider deriving `enumerateSubcommands`'s group set from the same registry data `TestTheHelpEnumerationIsComplete` already reads, instead of a third hardcoded list, so a new group fails with a clear message rather than a confusing double failure.
