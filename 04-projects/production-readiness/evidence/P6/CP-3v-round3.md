VERDICT: FAIL:escalate
LANE: full
CLAIMS_CHECKED: 12
EVIDENCE:
EVIDENCE AC-3b-classifier-dispatch-run-unverified | CP-3v | FAIL | `parseConfigFlagTable`'s name regex `` `([a-z]+(?: [a-z]+)?)` `` cannot match `` `dispatch <plan>` `` or `` `run <plan>` `` (the `<plan>` placeholder breaks the letters/space-only pattern). Confirmed programmatically: running the exact parser logic against the unmodified README table yields `len(buckets) == 18`, missing exactly `dispatch` and `run`. The `invocations` map in the test also has no `"dispatch"`/`"run"` keys. So `classifyConfigFlag` is never called for these two rows — the "Honoured" claim for the two commands mentioned first in the table's own README prose is completely unchecked by this guard, contrary to the code comment "they are exercised separately" (the only thing separately exercised is the positional-trap error-message string, which says nothing about honoured/ignored/rejected). | `/tmp/claude-1000/gloop-verify3`, `go run /tmp/claude-1000/counttest.go` output: count 18, no dispatch/run keys
EVIDENCE AC-3b-classifier-mutation-dispatch-run | CP-3v | FAIL | Mutation: moved `` `dispatch <plan>`, `run <plan>` `` from the Honoured row to the Ignored row in README.md (a false claim — dispatch/run do read `--config` per source, see `cmd/gloop/cmd/dispatch.go` calling `config.Load(configPath)` unconditionally). `go test ./internal/docguard/... -run TestTheConfigFlagTableMatchesTheBinary` **passed**. This is the exact defect class the phase exists to catch (a false bucket claim), surviving specifically in the two rows the parser cannot see. | mutated/reverted `/tmp/claude-1000/gloop-verify3/README.md`, test output captured this session
EVIDENCE AC-3b-parser-row-skip | CP-3v | FAIL | Mutation: changed the Rejected row's label cell from `**Rejected**` to `**Rejected.**` (one added period). `bucket := strings.Trim(strings.TrimSpace(cells[1]), "* ")` only trims asterisks/spaces, so the trailing `.` survives, `"rejected."` fails the switch, and the whole row (13 subcommands: `handoff get`, `handoff prune`, all 5 `gate` subs, both `roster` subs, all 3 `session` subs, `init`) is silently dropped from `buckets` with no error. `go test -run TestTheConfigFlagTableMatchesTheBinary` **passed**, and none of those 13 rows were exercised (confirmed by re-running the count script against the mutated file: 5 fewer... actually 0 rows for that bucket parsed). | mutated/reverted README.md, test output this session
EVIDENCE AC-3b-parser-backtick-drop | CP-3v | FAIL | Mutation: removed backticks from `` `session delete` `` inside the Rejected cell (left as plain `session delete`). The per-name regex requires literal backticks immediately around the token, so that one name silently drops out of `buckets` while its siblings in the same cell still parse. Test still **passed**; `session delete`'s row went unchecked with no diagnostic. | mutated/reverted README.md, test output this session
EVIDENCE AC-3b-round2-flip-now-caught | CP-3v | PASS | Re-ran round 2's exact case: moved `handoff prune` from Rejected to Ignored. Test now **fails** with `` `gloop handoff prune --config <path>` is rejected; README.md's table puts it under ignored, silently `` — the stated reason, not a compile error. | mutated/reverted README.md, `help_text_test.go:229` failure this session
EVIDENCE AC-3b-reverse-flip-caught | CP-3v | PASS | Reverse direction: moved `handoff list` from Ignored to Rejected. Test **fails** with `` `gloop handoff list --config <path>` is ignored, silently; README.md's table puts it under rejected ``. Both directions of a bucket flip are caught for rows the parser can see. | mutated/reverted README.md, this session
EVIDENCE AC-3b-uninvokable-row-caught | CP-3v | PASS | Added a bogus `` `session archive` `` entry to the Rejected cell. Test **fails**: `README.md's --config table names "session archive" and this test does not know how to invoke it, so its row is unchecked`. Confirms item 2 of the round-3 brief (a named-but-uninvokable subcommand is itself a failure) works as designed — for rows the parser reaches. | mutated/reverted README.md, this session
EVIDENCE AC-3b-completeness | CP-3v | PASS | Enumerated every leaf subcommand via `AddCommand` call sites and `--help` output: config{show,setup,update}(3) + gate{create,list,approve,reject,skip}(5) + handoff{list,get,prune}(3) + roster{show,validate}(2) + session{list,show,delete}(3) + status(1) + dispatch(1) + run(1) + init(1) = 20. The table names exactly 20 (5 Honoured incl. dispatch/run, 2 Ignored, 13 Rejected). All 20 are named in table text — completeness-by-naming holds — but per the classifier finding above, only 18 are behaviourally checked. | `grep -n AddCommand cmd/gloop/cmd/*.go`, `--help` output for config/gate/handoff/roster/session, this session
EVIDENCE AC-3b-build-record-count-misleading | CP-3v | FAIL | CP-3-build.md's closing line — "The table now names eighteen and the count is produced by the loop rather than asserted in prose" — is literally true (`len(buckets)==18`) but frames the loop's self-reported count as evidence of completeness, when the true reason it is 18 and not 20 is that the loop's own regex cannot see 2 of the table's named rows. The same rhetorical move ("N is a number the loop produces, not asserted in prose") that correctly closed round 2's "thirteen" gap now hides a new gap behind an accurate-sounding number. | reasoning from the two mutation results above plus the count script
EVIDENCE AC-3b-fixture-mutation-configshow | CP-3v | PASS | Mutated `TestConfigShowNeverContradictsItself`'s "valid" fixture write to contain `model = "WRONG-MODEL"` (fixture/assertion mismatch, same shape as the original bug this guard exists to catch). Test failed for the stated reason: `the valid fixture did not load: map[... model:WRONG-MODEL ...] -- this test would then be asserting nothing about the honoured path`. Not a hang, not a compile error. | mutated/reverted `internal/docguard/help_text_test.go`, this session
EVIDENCE AC-3b-liveindex-mutation | CP-3v | PASS | Mutated `docs/README.md`'s live-list link for `MOCKERY_INTEGRATION.md` to point elsewhere. `TestTheLiveListInTheIndexMatchesThisGuard` failed: `liveDocs names MOCKERY_INTEGRATION.md but docs/README.md's live list does not`. | mutated/reverted `docs/README.md`, this session
EVIDENCE AC-3b-sample-rest-of-claims | CP-3v | PASS | Direct binary/tree spot-checks: positional trap (`dispatch --config x plan.json` → `failed to read plan file: open --config: no such file or directory`, verbatim match); no `gloop gloop` or `gloop select` in `--help`; `roster validate` error offers only `gloop roster show <roster>`/`gloop roster validate <roster>`, no `roster plan`; README's `config.toml` example (extracted verbatim from the fenced block) loads cleanly via `config show --config <extracted file>`, echoing `provider: anthropic, model: claude-sonnet-4-20250514`; provider registry imports (anthropic, cohere, google, http, mistral, openai) all named in the README's Multi-Provider bullet. | binary runs at `/tmp/claude-1000/gloop-bin3`, `/tmp/claude-1000/readme-config-example.toml`, this session
FAILURES:
- AC-3b | `--config` table classifier coverage | `dispatch <plan>` and `run <plan>` — the two entries the table's own prose singles out for the positional-trap rule — are silently excluded from `TestTheConfigFlagTableMatchesTheBinary`'s classification loop because `parseConfigFlagTable`'s regex cannot match the `<plan>` placeholder inside backticks. A false bucket claim on either row (verified by mutation: moved both from Honoured to Ignored) passes the test.
- AC-3b | Table-parser robustness | The bucket-label match (`strings.Trim(cells[1], "* ")` then exact string compare) has no fallback/error path: any character outside `[* ]` in the label cell (a trailing period, differently-styled emphasis, a footnote marker) causes the entire row — all subcommands in it — to be silently dropped from testing, with no test failure, no log line, nothing. Same silent-drop behaviour for a single subcommand name that loses its backticks.
- AC-3b | CP-3-build.md accuracy | The "table now names eighteen" framing is not false but is not the safe reading it presents itself as: it is 18 out of 20 named rows because 2 are invisible to the parser, not because 18 is the table's full extent. A reader taking the sentence at face value would believe full coverage.
FIX_HINTS:
- AC-3b | Rewrite the row-name regex (or the table format) so `dispatch <plan>` and `run <plan>` parse into the buckets map like every other entry, and add both to the `invocations` map with a synthetic plan-file argument so `classifyConfigFlag` actually runs them. Until then, assert `len(buckets)` against the true subcommand count (20) rather than letting the loop silently accept whatever it manages to parse.
- AC-3b | Make bucket-label parsing fail loudly on an unrecognized non-empty label cell in what looks like a table row (e.g. `t.Errorf` on a line inside the table whose first cell is non-blank but doesn't map to a known bucket), instead of a bare `continue`, so a formatting slip surfaces instead of vanishing.
- AC-3b | Either drop the "eighteen ... produced by the loop" sentence or replace it with a sentence that ties the loop's count to the known total subcommand count so a shortfall is visible in prose, not just in behavior.
