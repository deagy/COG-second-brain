# P5 — CP-5 acceptance (AC-8)

EVIDENCE AC-8 | CP-5 | PASS | Full cycle against released `cli-v0.7.9`, checksum-verified: propose → disposition → `ingest-accepted` (1 chunk) → `cadre knowledge search` found it at score 0.578 → `delete-ingested` reported `chunks_removed: 1` → the identical search returned `"count": 0, "results": []` | CP-3v
EVIDENCE AC-8 | CP-5 | PASS | Absence confirmed **independently of cadre**: `recall store info` reported `chunks: 0`, asking the storage engine rather than asking cadre whether cadre's delete worked | CP-3v
EVIDENCE AC-8 | CP-5 | PASS | The evidence outlives its subject. `deletion-evidence` read the record back after the document was gone, from a physically separate database, carrying `document_id`, `chunks_removed: 1`, `reason`, `deleted_by`, `observed_actor`, `deleted_at` and an explicit `actor_verification` line | CP-3v
EVIDENCE AC-8 | CP-5 | PASS | The weak reading is refused. Deleting a nonexistent id, and re-deleting an already-deleted id, both exit 1 with *"holds no chunks in this store … Deleting nothing and recording that it happened would put a false entry"* — and the evidence afterwards showed exactly the one legitimate record, the failed attempts adding nothing | CP-3v
EVIDENCE AC-8 | CP-5 | PASS | Against a server-backed store, the deletion record carries `subject:alice@corp.example` with `actor_verification: "verified"`, distinct from the caller-typed `--deleted-by` — so P5's evidence writer takes its actor from the resolver P3 built rather than inventing a second answer | CP-4
EVIDENCE AC-8 | CP-5 | PASS | Retired verbs still refuse by name rather than crashing: `ingest` names `recall upload`, `retention-report` names its own Python-era removal. Retention windows remain out of scope by charter, so `retention-report` refusing after this phase is correct rather than incomplete | CP-3v
