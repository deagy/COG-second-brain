# 2026-09-02

## Log

### 08:35 — P4 closed: the absent capability now refuses where it is reached for
- **Focus:** production-readiness
- **Did:** Closed AC-5. The retired retention and deletion *verbs* already refused by name; their *flags* answered `flag provided but not defined`, which is a parser fact delivered at the moment someone is trying to honour an obligation. `--retention-days`, `--trigger` and `--as-of` now refuse pre-parse on both dispatch routes, naming what was removed, the commit, that nothing rebuilt it, and that the decision is open.
- **Artifacts:** cadre `0e249942`, 04-projects/production-readiness/evidence/P4/{CP-4-integration.md,CP-5-acceptance.md}
- **Signal:** CP-4 caught the thing that mattered more than the feature — the pre-parse scan read a flag's *value* as a request, so `delete-staged --reason "--retention-days ..."` refused a working command. A message improvement that breaks a command is not an improvement. I named that exact risk in the CP-4 brief and shipped it anyway: the reasoning was in the brief and not in the code.

### 09:15 — P5: four repositories released, and the container found two defects nothing else could
- **Focus:** production-readiness
- **Did:** Cut cadre-kernel v0.14.3 then v0.14.4, recall v0.3.3, cadre cli-v0.7.0/plugin-v0.24.0 then cli-v0.7.1/plugin-v0.24.1. Settled gloop on evidence: its documented `go install …@latest` cannot work against a private repository (proxy and pkg.go.dev both 404), so the README now says clone and `make install`, and states in the repo why there are no releases.
- **Did:** Fixed recall's tag pipeline, which had two independent reasons for publishing nothing: a tag pushed with the workflow's own `GITHUB_TOKEN` triggers no further workflow, and the release job lacked the cadre checkout its fail-closed contract guard needs — a fix that had landed in `go.yml` and not in `release.yml`.
- **Artifacts:** cadre `9e9bd6ab`, cadre-kernel `v0.14.4`, recall `v0.3.3`, gloop `3715bef`, 04-projects/production-readiness/evidence/P5/
- **Signal:** **The kernel shim could not install a kernel, on any release since the extraction.** It resolves a checksum with `grep " $ARCHIVE$"` and every release wrote `SHA256SUMS` with `sha256sum ./*`, so no line ever matched; it reported the archive as unlisted, which reads as a broken release rather than a producer and its only consumer disagreeing about a format. Invisible from any machine that already has a kernel. I had also looked straight at it while verifying v0.14.3 — printed the `./`-prefixed line beside the bare filename and read only the hashes.
- **Signal:** A `cadre` installed by `install.sh` needed a Go toolchain that INSTALL.md never mentions. The checkout already ships the packaged launcher that downloads a released binary; `bin/cadre` now falls back to it instead of refusing.
- **Signal:** One constant moved and dragged five places with it. Four were found by guards; the fifth, `CADRE_KERNEL_REF` in `validate.yml`, was found by a red runner *after* cli-v0.7.0 had already published — the release job and the validate job run on the same push and neither waits for the other. Added a guard that compares the workflow pin to the constant locally, so it fails before the push rather than after it.

### P6, and the north-star gate reopening a criterion six phases old

- **Did:** Closed AC-3b — a claim-by-claim audit of gloop's documentation against the built binary. 22 false claims corrected, 20 guards built, 22 records under `docs/` given historical banners. Nine CP-3v rounds, an escalation the user resolved, and CP-4 found a guard I had built and falsified that my own scripted rewrite had deleted five rounds earlier.
- **Did:** Ran the north-star gate. It failed AC-8. P7 corrected `#249`'s body and deleted six `kernel-v*` releases from the cadre repo; CP-4 then found nine live documents still installing the kernel from the retired home.
- **Artifacts:** gloop `1f37de4`, cadre `cd836b95`, `04-projects/production-readiness/evidence/P6/` and `/P7/`, retro `2026-09-02-production-readiness.md`, AI-19…AI-29
- **Signal:** **AC-8 carried a PASS row for six phases while two of its three clauses were false.** P2 verified that `release.yml` no longer publishes kernel releases; the criterion says the kernel has one release *home*, which is a claim about artifacts, and six were still live and downloadable. P2 also read a closing *comment* on `#249` as the issue body being corrected — identical to anyone reading the thread top to bottom, different to anyone who lands on the body from a search result. Verify the class of artifact the criterion names, not the machinery that produces it.
- **Signal:** The gate's own FAIL cited `curl` on `releases/tag/<tag>` returning 200 as proof a release was live. That URL still returns 200 with the releases deleted, because GitHub renders a page for a bare git tag. Right verdict, weak citation — the combination nobody re-reads.
- **Signal:** P6's escalation was the pivot, and it took three rounds to make. Three attempts to guard gloop's `--config` table all parsed the README and all disagreed with it for a new reason. The method the user chose inverted the guard's direction — generate the expected table from the binary and assert the README contains it — rather than refining the parse. A guard that parses a document to check it will keep disagreeing with the document.
- **Signal:** The provider list drifted in six places and took five rounds to fix, because each guard covered the list in front of it. It stopped when the guard began *finding* enumerations rather than naming them. That is AI-16, a rule I wrote and broke twice in the phase that most needed it.
- **Signal:** My CI-polling loop exited immediately on an empty `gh run list`, reading "no runs yet" as "finished". Same shape as everything above: a silent omission whose absence is indistinguishable from success.

### production-readiness — closed

- **What:** North-star gate COMPLETE on all nine criteria after a second run. The goal ran eight phases across two days: kernel licensed, gloop settled internal and made true, `#249` closed, four actor fields derived-or-refuse, ~15 retention reach-paths refusing by name, all four released and a clean machine proven to reach a working state, gloop's docs audited claim by claim, and AC-8 reopened and closed.
- **Artifacts:** `04-projects/production-readiness/` (spec, STATUS, `evidence/P0`–`P7`, `evidence/northstar-gate.md`, `report.html`); cadre `bffec5cc`, cadre-kernel `v0.14.4`, recall `v0.3.3`, gloop `1f37de4`
- **Signal:** **The gate's first run earned the whole apparatus.** It failed AC-8 six phases after P2 closed it — P2 had verified the publishing workflow, and the criterion named the artifacts, six of which were still live and downloadable. Nothing between P2 and the gate could have caught that: every intervening check was scoped to its own phase, and a phase cannot see what it already believes it verified.
- **Signal:** The gate passed nine of nine and then said the north-star sentence was false, which is the second time in two days a gate has done that. It named gloop alongside three subjects that install from published artifacts; gloop installs from a checkout, by the decision P1 was chartered to make on evidence. The sentence predated its own open question being answered. Amended — but put to the user rather than taken, because a worker rewriting the sentence it is judged by is the failure the harness has a rule against, and the amendment had to pass two tests: nothing about the software changed to accommodate it, and the alternative (publishing a release with no consumer) was worse on the goal's own terms.
- **Signal:** Deleting the six kernel releases breaks the kernel fetch of eight published CLI releases. I found that by reading the shim out of the extracted `cli-v0.6.5` binary rather than inferring it from git, put it to the user before doing anything irreversible, and they accepted the cost on the charter's bar of one operator running HEAD.
- **Signal:** My doc fix went red on the runner while the full Go suite passed locally, and the instruction I had followed was the reason. `AGENTS.md` named `go test ./internal/generators/` as the regeneration guard; that test proves the generator is deterministic against a temp directory, and only `cadre generate-plugin --check` against the committed tree catches a hand-edit — run by `validate.yml`, by no test. The guard the contributor document names and the guard that gates the merge were different checks, and the named one passes on exactly the defect the real one catches.
- **Signal:** Found and deliberately not fixed: `cadre-kernel/kernel/README.md` opens with a correct warning that the kernel is a Go binary, then spends its lower half instructing `python3 -m agentic_sdlc` and `pip install -r kernel/requirements-validation.txt` in a repository with zero `.py` files. It is AC-3b's shape applied to a repository AC-3b does not cover. Recording it beat growing a ninth phase nobody chartered — but it is a live falsehood on a public Apache-2.0 repo's landing document and wants its own small goal.
Harvest: 6 staged, AI-19..AI-30 filed with dispositions, 6 rules landed in skill bodies

### The three unbuilt controls, and the kernel README

- **What:** Built AI-19, AI-20 and AI-28 as real checks, each falsified in both directions, and made `cadre-kernel`'s landing document true.
- **Artifacts:** `.claude/lib/release-hygiene.sh` and `evidence-lint.sh` § machinery (vault `19bbaf3`), wired into the ultragoal gate; cadre-kernel `c9500b6`
- **Signal:** **Two of the three dispositions were wrong about their own check, and building them is the only thing that could have shown it.** AI-28 proposed firing on the cited artifact's file extension — built that way it fires on legitimate component rows and misses the precision case entirely. What actually separates the defect from correct work is whether the *observation* is about publishing machinery while the criterion names the published thing. Built that way it finds P2's row and nothing else across four goals. AI-20 proposed four per-repository suite assertions, none of which can see an exception or notice a repository *gaining* a licence.
- **Signal:** The machinery check found P2's row on its first run, which is the row that was wrong for six phases. I marked the verdict `SUPERSEDED` and left the observation exactly as written — what it saw was true, the inference to AC-8 was not. That distinction is worth keeping: correcting the observation would have destroyed the evidence of how the mistake was made.
- **Signal:** `cadre-kernel/kernel/README.md` opened with a correct warning that the kernel is a Go binary and then spent its lower half instructing the reader in Python — eight separate claims, in a repository with zero `.py` files. Four more pointed at a LangGraph engine and an `agentic-sdlc-lg` CLI that exist in none of the four repositories. One demo step I wrote to replace them was itself wrong (separation is enforced at the routing layer by `plan`/`validate`, not by `decide` refusing an actor) — caught by checking before asserting, which is the only reason it isn't the next entry here.
- **Signal:** Three retros now share the `AI-19`–`AI-26` range in one backlog table. Ids are declared retro-scoped so the lint passes, but a grep for `AI-22` returns two unrelated rows with different dispositions, and it misled me for a full turn. Flagged in the header rather than renumbered, which would break every citation.
- **Blocked:** four scratch worktrees still registered. `git worktree remove` is refused by a hook as a destructive git-metadata operation; routing around it with `rm -rf` would defeat a control that was installed deliberately, so it needs a human hand.
